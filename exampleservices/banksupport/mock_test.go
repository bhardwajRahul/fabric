// Code generated by cmd/genservice. DO NOT EDIT.

package banksupport

import (
	"context"
	"net/http"
	"testing"

	"github.com/microbus-io/dwarf/workflow"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/exampleservices/banksupport/banksupportapi"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/testarossa"
)

func TestBanksupport_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("balance", func(t *testing.T) { // MARKER: Balance
		assert := testarossa.For(t)

		mock.MockBalance(func(ctx context.Context) (balanceCents int, holder string, err error) {
			return
		})
		_, _, err := mock.Balance(ctx)
		assert.NoError(err)
	})

	t.Run("transactions", func(t *testing.T) { // MARKER: Transactions
		assert := testarossa.For(t)

		mock.MockTransactions(func(ctx context.Context, fromDate string, toDate string) (transactions []banksupportapi.TxnView, err error) {
			return
		})
		var fromDate string
		var toDate string
		_, err := mock.Transactions(ctx, fromDate, toDate)
		assert.NoError(err)
	})

	t.Run("demo_status", func(t *testing.T) { // MARKER: DemoStatus
		assert := testarossa.For(t)

		mock.MockDemoStatus(func(ctx context.Context, flowKey string) (status string, advice string, blockCard bool, risk int, errorMsg string, err error) {
			return
		})
		var flowKey string
		_, _, _, _, _, err := mock.DemoStatus(ctx, flowKey)
		assert.NoError(err)
	})

	t.Run("login", func(t *testing.T) { // MARKER: Login
		assert := testarossa.For(t)

		mock.MockLogin(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.Login(w, r)
		assert.NoError(err)
	})

	t.Run("logout", func(t *testing.T) { // MARKER: Logout
		assert := testarossa.For(t)

		mock.MockLogout(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.Logout(w, r)
		assert.NoError(err)
	})

	t.Run("demo", func(t *testing.T) { // MARKER: Demo
		assert := testarossa.For(t)

		mock.MockDemo(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.Demo(w, r)
		assert.NoError(err)
	})

	t.Run("run_support", func(t *testing.T) { // MARKER: RunSupport
		assert := testarossa.For(t)

		mock.MockRunSupport(func(ctx context.Context, flow *workflow.Flow, query string) (advice string, blockCard bool, risk int, err error) {
			return
		})
		var query string
		_, _, _, err := mock.RunSupport(ctx, nil, query)
		assert.NoError(err)
	})

	t.Run("support", func(t *testing.T) { // MARKER: Support
		assert := testarossa.For(t)

		mock.MockSupport(func(ctx context.Context, flow *workflow.Flow, query string) (advice string, blockCard bool, risk int, err error) {
			return
		})
		graph, err := mock.Support(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

}
