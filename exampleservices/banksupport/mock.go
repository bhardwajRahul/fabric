// Code generated by cmd/genservice. DO NOT EDIT.

package banksupport

import (
	"context"
	"encoding/json"
	"net/http"

	"github.com/microbus-io/dwarf/workflow"
	"github.com/microbus-io/errors"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/exampleservices/banksupport/banksupportapi"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/fabric/sub"
	"github.com/microbus-io/fabric/utils"
)

// Mock is a mockable version of the microservice, allowing functions, event sinks and web handlers to be mocked.
type Mock struct {
	*Intermediate
	mockBalance      func(ctx context.Context) (balanceCents int, holder string, err error)                                                         // MARKER: Balance
	mockTransactions func(ctx context.Context, fromDate string, toDate string) (transactions []banksupportapi.TxnView, err error)                   // MARKER: Transactions
	mockDemoStatus   func(ctx context.Context, flowKey string) (status string, advice string, blockCard bool, risk int, errorMsg string, err error) // MARKER: DemoStatus
	mockLogin        func(w http.ResponseWriter, r *http.Request) (err error)                                                                       // MARKER: Login
	mockLogout       func(w http.ResponseWriter, r *http.Request) (err error)                                                                       // MARKER: Logout
	mockDemo         func(w http.ResponseWriter, r *http.Request) (err error)                                                                       // MARKER: Demo
	mockRunSupport   func(ctx context.Context, flow *workflow.Flow, query string) (advice string, blockCard bool, risk int, err error)              // MARKER: RunSupport
	mockSupportGraph func(ctx context.Context) (graph *workflow.Graph, err error)                                                                   // MARKER: Support
	unsubMockSupport func() error                                                                                                                   // MARKER: Support
}

// NewMock creates a new mockable version of the microservice.
func NewMock() *Mock {
	svc := &Mock{}
	svc.Intermediate = NewIntermediate(svc)
	svc.SetVersion(7357) // Stands for TEST
	return svc
}

// OnStartup is called when the microservice is started up.
func (svc *Mock) OnStartup(ctx context.Context) (err error) {
	if svc.Deployment() != connector.LOCAL && svc.Deployment() != connector.TESTING {
		return errors.New("mocking disallowed in %s deployment", svc.Deployment())
	}
	return nil
}

// OnShutdown is called when the microservice is shut down.
func (svc *Mock) OnShutdown(ctx context.Context) (err error) {
	return nil
}

// MockBalance sets up a mock handler for Balance.
func (svc *Mock) MockBalance(handler func(ctx context.Context) (balanceCents int, holder string, err error)) *Mock { // MARKER: Balance
	svc.mockBalance = handler
	return svc
}

// Balance executes the mock handler.
func (svc *Mock) Balance(ctx context.Context) (balanceCents int, holder string, err error) { // MARKER: Balance
	if svc.mockBalance != nil {
		balanceCents, holder, err = svc.mockBalance(ctx)
	}
	return balanceCents, holder, errors.Trace(err)
}

// MockTransactions sets up a mock handler for Transactions.
func (svc *Mock) MockTransactions(handler func(ctx context.Context, fromDate string, toDate string) (transactions []banksupportapi.TxnView, err error)) *Mock { // MARKER: Transactions
	svc.mockTransactions = handler
	return svc
}

// Transactions executes the mock handler.
func (svc *Mock) Transactions(ctx context.Context, fromDate string, toDate string) (transactions []banksupportapi.TxnView, err error) { // MARKER: Transactions
	if svc.mockTransactions != nil {
		transactions, err = svc.mockTransactions(ctx, fromDate, toDate)
	}
	return transactions, errors.Trace(err)
}

// MockDemoStatus sets up a mock handler for DemoStatus.
func (svc *Mock) MockDemoStatus(handler func(ctx context.Context, flowKey string) (status string, advice string, blockCard bool, risk int, errorMsg string, err error)) *Mock { // MARKER: DemoStatus
	svc.mockDemoStatus = handler
	return svc
}

// DemoStatus executes the mock handler.
func (svc *Mock) DemoStatus(ctx context.Context, flowKey string) (status string, advice string, blockCard bool, risk int, errorMsg string, err error) { // MARKER: DemoStatus
	if svc.mockDemoStatus != nil {
		status, advice, blockCard, risk, errorMsg, err = svc.mockDemoStatus(ctx, flowKey)
	}
	return status, advice, blockCard, risk, errorMsg, errors.Trace(err)
}

// MockLogin sets up a mock handler for Login.
func (svc *Mock) MockLogin(handler func(w http.ResponseWriter, r *http.Request) (err error)) *Mock { // MARKER: Login
	svc.mockLogin = handler
	return svc
}

// Login executes the mock handler.
func (svc *Mock) Login(w http.ResponseWriter, r *http.Request) (err error) { // MARKER: Login
	if svc.mockLogin != nil {
		err = svc.mockLogin(w, r)
	}
	return errors.Trace(err)
}

// MockLogout sets up a mock handler for Logout.
func (svc *Mock) MockLogout(handler func(w http.ResponseWriter, r *http.Request) (err error)) *Mock { // MARKER: Logout
	svc.mockLogout = handler
	return svc
}

// Logout executes the mock handler.
func (svc *Mock) Logout(w http.ResponseWriter, r *http.Request) (err error) { // MARKER: Logout
	if svc.mockLogout != nil {
		err = svc.mockLogout(w, r)
	}
	return errors.Trace(err)
}

// MockDemo sets up a mock handler for Demo.
func (svc *Mock) MockDemo(handler func(w http.ResponseWriter, r *http.Request) (err error)) *Mock { // MARKER: Demo
	svc.mockDemo = handler
	return svc
}

// Demo executes the mock handler.
func (svc *Mock) Demo(w http.ResponseWriter, r *http.Request) (err error) { // MARKER: Demo
	if svc.mockDemo != nil {
		err = svc.mockDemo(w, r)
	}
	return errors.Trace(err)
}

// MockRunSupport sets up a mock handler for RunSupport.
func (svc *Mock) MockRunSupport(handler func(ctx context.Context, flow *workflow.Flow, query string) (advice string, blockCard bool, risk int, err error)) *Mock { // MARKER: RunSupport
	svc.mockRunSupport = handler
	return svc
}

// RunSupport executes the mock handler.
func (svc *Mock) RunSupport(ctx context.Context, flow *workflow.Flow, query string) (advice string, blockCard bool, risk int, err error) { // MARKER: RunSupport
	if svc.mockRunSupport != nil {
		advice, blockCard, risk, err = svc.mockRunSupport(ctx, flow, query)
	}
	return advice, blockCard, risk, errors.Trace(err)
}

// MockSupport sets up a mock handler for the Support workflow.
// The handler receives typed inputs from the workflow's state and returns typed outputs.
// A nil handler clears the mock.
func (svc *Mock) MockSupport(handler func(ctx context.Context, flow *workflow.Flow, query string) (advice string, blockCard bool, risk int, err error)) *Mock { // MARKER: Support
	if svc.unsubMockSupport != nil {
		svc.unsubMockSupport()
		svc.unsubMockSupport = nil
	}
	if handler == nil {
		svc.mockSupportGraph = nil
		return svc
	}
	mockName := "MockSupport" + utils.RandomIdentifier(8)
	mockRoute := ":428/mock-support-" + utils.RandomIdentifier(8)
	mockTaskURL := httpx.JoinHostAndPath(svc.Hostname(), mockRoute)
	svc.mockSupportGraph = func(ctx context.Context) (graph *workflow.Graph, err error) {
		g := workflow.NewGraph("Support")
		g.AddTransition(mockTaskURL, workflow.END)
		return g, nil
	}
	err := svc.Subscribe(mockName, func(w http.ResponseWriter, r *http.Request) error {
		var f workflow.Flow
		if err := json.NewDecoder(r.Body).Decode(&f); err != nil {
			return errors.Trace(err)
		}
		snap := f.Snapshot()
		var in banksupportapi.SupportIn
		f.ParseState(&in)
		advice, blockCard, risk, err := handler(r.Context(), &f, in.Query)
		if err != nil {
			return err // No trace
		}
		out := banksupportapi.SupportOut{Advice: advice, BlockCard: blockCard, Risk: risk}
		f.SetChanges(out, snap)
		w.Header().Set("Content-Type", "application/json")
		return json.NewEncoder(w).Encode(&f)
	},
		sub.At("POST", mockRoute),
		sub.Task(banksupportapi.SupportIn{}, banksupportapi.SupportOut{}),
	)
	if err == nil {
		svc.unsubMockSupport = func() error { return svc.Unsubscribe(mockName) }
	}
	return svc
}

// Support returns the workflow graph, or a mocked graph if MockSupport was called.
func (svc *Mock) Support(ctx context.Context) (graph *workflow.Graph, err error) { // MARKER: Support
	if svc.mockSupportGraph != nil {
		graph, err = svc.mockSupportGraph(ctx)
	}
	return graph, errors.Trace(err)
}
