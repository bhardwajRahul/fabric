# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: BankSupport
  hostname: banksupport.example
  description: BankSupport is an LLM agent that answers a signed-in customer's banking questions using their own balance and transactions as tools, gated by actor-claims authentication.
  package: github.com/microbus-io/fabric/exampleservices/banksupport
  modifiedAt: "2026-07-05T21:42:17Z"

functions:
  Balance:
    signature: Balance() (balanceCents int, holder string)
    description: |-
      Balance returns the signed-in customer's current balance. It derives the customer from the actor claim, never
      from an argument, and is exposed to the LLM as a tool.
    method: ANY
    route: /balance
    requiredClaims: roles.customer
  Transactions:
    signature: Transactions(fromDate string, toDate string) (transactions []TxnView)
    description: |-
      Transactions returns the signed-in customer's transactions in a date range. It derives the customer from the
      actor claim, never from an argument, and is exposed to the LLM as a tool for spend analysis over time.
    method: ANY
    route: /transactions
    requiredClaims: roles.customer
  DemoStatus:
    signature: DemoStatus(flowKey string) (status string, advice string, blockCard bool, risk int, errorMsg string)
    description: |-
      DemoStatus long-polls a launched Support workflow via the foreman's Poll and returns the structured verdict
      when the flow has stopped, or a running status so the demo page can re-poll immediately with no client delay.
    method: ANY
    route: /demo-status
    requiredClaims: roles.customer

webs:
  Login:
    description: Login renders the login screen and, on valid credentials, mints a customer bearer token and redirects to the demo.
    method: ANY
    route: /login
  Logout:
    description: Logout clears the customer session cookie and returns to the login screen.
    method: ANY
    route: /logout
  Demo:
    description: Demo is the signed-in support console where a customer asks natural-language banking questions.
    method: ANY
    route: /demo
    requiredClaims: roles.customer

tasks:
  RunSupport:
    signature: RunSupport(query string) (advice string, blockCard bool, risk int)
    description: |-
      RunSupport is the single task of the Support workflow. It runs llm.core's ChatLoop as a subgraph with the
      Balance and Transactions tools and parses the model's final message into the structured verdict.
    route: :428/run-support

workflows:
  Support:
    signature: Support(query string) (advice string, blockCard bool, risk int)
    description: |-
      Support is the durable workflow that answers a customer's banking question. Its RunSupport task runs the LLM
      tool-calling loop (ChatLoop) with the Balance and Transactions endpoints as tools and produces a structured
      verdict: advice text, whether the card should be blocked, and a 0-10 risk score. It is a workflow rather than a
      synchronous call because the multi-turn agent conversation routinely exceeds a single request's time budget;
      each turn is its own durable, independently-budgeted foreman step.
    route: :428/support
