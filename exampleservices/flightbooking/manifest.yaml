# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: FlightBooking
  hostname: flightbooking.example
  description: FlightBooking is an example agentic workflow that searches flights, pauses for a human accept/keep-searching decision via Interrupt/Resume, and delegates seat selection to a child LLM workflow.
  package: github.com/microbus-io/fabric/exampleservices/flightbooking
  modifiedAt: "2026-07-05T15:35:57Z"

webs:
  Demo:
    description: |-
      Demo serves the human-in-the-loop demo page that drives the BookFlight workflow, presenting each proposed
      flight and resuming the parked flow with the traveler's accept or keep-searching decision.
    method: ANY
    route: /demo

tasks:
  SearchFlights:
    signature: SearchFlights(origin string, destination string) (candidates []Flight, flightIndex int)
    description: SearchFlights looks up the flights serving a route and seeds the candidate list for the workflow to propose.
    route: :428/search-flights
  ProposeFlight:
    signature: ProposeFlight(candidates []Flight, flightIndex int) (currentFlight Flight, exhausted bool)
    description: ProposeFlight selects the candidate at the current index, or marks the search exhausted when the list runs out.
    route: :428/propose-flight
  AwaitDecision:
    signature: AwaitDecision(currentFlight Flight, flightIndex int) (accepted bool, flightIndexOut int)
    description: |-
      AwaitDecision parks the flow on a human accept/keep-searching decision for the proposed flight and advances
      to the next candidate when the traveler keeps searching.
    route: :428/await-decision
  ChooseSeat:
    signature: ChooseSeat(seatPreference string, currentFlight Flight) (seat string)
    description: ChooseSeat delegates seat selection for the accepted flight to the ChooseSeatAgent child workflow.
    route: :428/choose-seat
  ConfirmBooking:
    signature: ConfirmBooking(currentFlight Flight, seat string) (confirmation string, airline string, flightNo string)
    description: ConfirmBooking finalizes the booking of the accepted flight and chosen seat into a confirmation.
    route: :428/confirm-booking
  NoFlights:
    signature: NoFlights() (confirmation string)
    description: |-
      NoFlights ends the workflow with a not-booked message when no flight is available or every candidate is
      declined.
    route: :428/no-flights
  PickSeat:
    signature: PickSeat(seatPreference string, availableSeats []string) (seat string)
    description: PickSeat asks the LLM to choose one seat from the available list that best matches the traveler's preference.
    route: :428/pick-seat

workflows:
  BookFlight:
    signature: BookFlight(origin string, destination string, seatPreference string) (confirmation string, airline string, flightNo string, seat string)
    description: |-
      BookFlight is the top-level agentic workflow. It searches the route, then loops proposing one candidate flight
      at a time and parking on a human accept/keep-searching decision via Interrupt. On acceptance it invokes the
      ChooseSeatAgent child workflow as an isolated subgraph to pick a seat, then confirms the booking.
    route: :428/book-flight
  ChooseSeatAgent:
    signature: ChooseSeatAgent(seatPreference string, availableSeats []string) (seat string)
    description: |-
      ChooseSeatAgent is a child workflow that selects a single seat from a flight's available seats to match a
      natural-language preference. It is invoked as an isolated subgraph by the BookFlight workflow's ChooseSeat task.
    route: :428/choose-seat-agent
