# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: Weather
  hostname: weather.example
  description: Weather is an LLM agent that answers natural-language weather questions by geocoding a location and fetching its forecast through sequential tool-calling.
  package: github.com/microbus-io/fabric/exampleservices/weather
  modifiedAt: "2026-07-02T20:08:14Z"

functions:
  LatLng:
    signature: LatLng(location string) (lat float64, lng float64)
    description: LatLng geocodes a location name into its latitude and longitude coordinates.
    method: ANY
    route: /lat-lng
  Forecast:
    signature: Forecast(lat float64, lng float64) (summary string, temperatureC float64, precipitationChance int)
    description: Forecast returns the current weather forecast for a latitude/longitude coordinate.
    method: ANY
    route: /forecast

tasks:
  Answer:
    signature: Answer(question string) (answer string)
    description: Answer runs the LLM tool-calling loop for a weather question, chaining the LatLng and Forecast tools, and returns the agent's reply.
    route: :428/answer

workflows:
  AskAgent:
    signature: AskAgent(question string) (answer string)
    description: AskAgent answers a natural-language weather question. It runs the LLM tool-calling loop as a durable workflow, letting the model geocode a location with LatLng and fetch its forecast with Forecast before composing a reply.
    route: :428/ask-agent
