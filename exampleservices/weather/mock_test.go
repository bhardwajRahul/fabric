// Code generated by cmd/genservice. DO NOT EDIT.

package weather

import (
	"context"
	"testing"

	"github.com/microbus-io/dwarf/workflow"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/testarossa"
)

func TestWeather_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("lat_lng", func(t *testing.T) { // MARKER: LatLng
		assert := testarossa.For(t)

		mock.MockLatLng(func(ctx context.Context, location string) (lat float64, lng float64, err error) {
			return
		})
		var location string
		_, _, err := mock.LatLng(ctx, location)
		assert.NoError(err)
	})

	t.Run("forecast", func(t *testing.T) { // MARKER: Forecast
		assert := testarossa.For(t)

		mock.MockForecast(func(ctx context.Context, lat float64, lng float64) (summary string, temperatureC float64, precipitationChance int, err error) {
			return
		})
		var lat float64
		var lng float64
		_, _, _, err := mock.Forecast(ctx, lat, lng)
		assert.NoError(err)
	})

	t.Run("answer", func(t *testing.T) { // MARKER: Answer
		assert := testarossa.For(t)

		mock.MockAnswer(func(ctx context.Context, flow *workflow.Flow, question string) (answer string, err error) {
			return
		})
		var question string
		_, err := mock.Answer(ctx, nil, question)
		assert.NoError(err)
	})

	t.Run("ask_agent", func(t *testing.T) { // MARKER: AskAgent
		assert := testarossa.For(t)

		mock.MockAskAgent(func(ctx context.Context, flow *workflow.Flow, question string) (answer string, err error) {
			return
		})
		graph, err := mock.AskAgent(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

}
