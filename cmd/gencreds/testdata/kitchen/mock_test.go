// Code generated by cmd/genservice. DO NOT EDIT.

package kitchen

import (
	"context"
	"testing"

	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/testarossa"
)

func TestKitchen_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("my_func", func(t *testing.T) { // MARKER: MyFunc
		assert := testarossa.For(t)

		mock.MockMyFunc(func(ctx context.Context, input string) (output string, err error) {
			return
		})
		var input string
		_, err := mock.MyFunc(ctx, input)
		assert.NoError(err)
	})

	t.Run("self_ping", func(t *testing.T) { // MARKER: SelfPing
		assert := testarossa.For(t)

		mock.MockSelfPing(func(ctx context.Context) (err error) {
			return
		})
		err := mock.SelfPing(ctx)
		assert.NoError(err)
	})

	t.Run("alt_host_fn", func(t *testing.T) { // MARKER: AltHostFn
		assert := testarossa.For(t)

		mock.MockAltHostFn(func(ctx context.Context) (err error) {
			return
		})
		err := mock.AltHostFn(ctx)
		assert.NoError(err)
	})

	t.Run("on_something", func(t *testing.T) { // MARKER: OnSomething
		assert := testarossa.For(t)

		mock.MockOnSomething(func(ctx context.Context, detail string) (ok bool, err error) {
			return
		})
		var detail string
		_, err := mock.OnSomething(ctx, detail)
		assert.NoError(err)
	})

	t.Run("on_observe_queue_depth", func(t *testing.T) { // MARKER: QueueDepth
		assert := testarossa.For(t)

		mock.MockOnObserveQueueDepth(func(ctx context.Context) (err error) {
			return
		})
		err := mock.OnObserveQueueDepth(ctx)
		assert.NoError(err)
	})

	t.Run("on_changed_threshold", func(t *testing.T) { // MARKER: Threshold
		assert := testarossa.For(t)

		mock.MockOnChangedThreshold(func(ctx context.Context) (err error) {
			return
		})
		err := mock.OnChangedThreshold(ctx)
		assert.NoError(err)
	})

}
