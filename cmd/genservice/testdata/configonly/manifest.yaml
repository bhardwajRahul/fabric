# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: ConfigOnly
  hostname: configonly.example
  description: The configonly microservice is a fixture exercising an endpoint-less (config-only) service.
  package: github.com/microbus-io/fabric/cmd/genservice/testdata/configonly
  modifiedAt: "2026-06-19T00:00:00Z"

configs:
  Threshold:
    signature: Threshold() (value int)
    description: Threshold is a plain integer config.
    validation: int [0,100]
    default: 10
  DenyList:
    signature: DenyList() (value string)
    description: DenyList is a multi-line config default; changes fire OnChangedDenyList.
    default: |-
      /admin
      /.git
    callback: true
  Retry:
    signature: Retry() (value RetryPolicy)
    description: |-
      Retry is a structured (JSON) config whose value carrier is a struct, exercising the json getter/setter
      and the qualification of the value type into the service package.
    validation: json
    default: "{\"maxRetries\":3,\"backoff\":\"1s\"}"

metrics:
  ReconcileDuration:
    signature: ReconcileDuration(value time.Duration)
    description: |-
      ReconcileDuration records how long a reconciliation took, as a duration. Its time.Duration value type
      is the only thing in this fixture that requires the time import in the generated recorder.
    kind: histogram
    buckets: [0.1, 0.5, 1, 5]
    otelName: configonly_reconcile_duration
