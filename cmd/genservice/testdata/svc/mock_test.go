// Code generated by cmd/genservice. DO NOT EDIT.

package svc

import (
	"context"
	"net/http"
	"net/url"
	"testing"
	"time"

	"github.com/microbus-io/dwarf/workflow"
	"github.com/microbus-io/fabric/cmd/genservice/testdata/svc/svcapi"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/testarossa"
)

func TestSvc_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("greet", func(t *testing.T) { // MARKER: Greet
		assert := testarossa.For(t)

		mock.MockGreet(func(ctx context.Context, name string) (greeting string, err error) {
			return
		})
		var name string
		_, err := mock.Greet(ctx, name)
		assert.NoError(err)
	})

	t.Run("adopt", func(t *testing.T) { // MARKER: Adopt
		assert := testarossa.For(t)

		mock.MockAdopt(func(ctx context.Context, pet svcapi.Pet) (since time.Time, err error) {
			return
		})
		var pet svcapi.Pet
		_, err := mock.Adopt(ctx, pet)
		assert.NoError(err)
	})

	t.Run("ping", func(t *testing.T) { // MARKER: Ping
		assert := testarossa.For(t)

		mock.MockPing(func(ctx context.Context) (err error) {
			return
		})
		err := mock.Ping(ctx)
		assert.NoError(err)
	})

	t.Run("dashboard", func(t *testing.T) { // MARKER: Dashboard
		assert := testarossa.For(t)

		mock.MockDashboard(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.Dashboard(w, r)
		assert.NoError(err)
	})

	t.Run("status", func(t *testing.T) { // MARKER: Status
		assert := testarossa.For(t)

		mock.MockStatus(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.Status(w, r)
		assert.NoError(err)
	})

	t.Run("upload", func(t *testing.T) { // MARKER: Upload
		assert := testarossa.For(t)

		mock.MockUpload(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.Upload(w, r)
		assert.NoError(err)
	})

	t.Run("process_step", func(t *testing.T) { // MARKER: ProcessStep
		assert := testarossa.For(t)

		mock.MockProcessStep(func(ctx context.Context, flow *workflow.Flow, item string) (done bool, err error) {
			return
		})
		var item string
		_, err := mock.ProcessStep(ctx, nil, item)
		assert.NoError(err)
	})

	t.Run("review_step", func(t *testing.T) { // MARKER: ReviewStep
		assert := testarossa.For(t)

		mock.MockReviewStep(func(ctx context.Context, flow *workflow.Flow, count int) (countOut int, err error) {
			return
		})
		var count int
		_, err := mock.ReviewStep(ctx, nil, count)
		assert.NoError(err)
	})

	t.Run("main_flow", func(t *testing.T) { // MARKER: MainFlow
		assert := testarossa.For(t)

		mock.MockMainFlow(func(ctx context.Context, flow *workflow.Flow, item string, pet svcapi.Pet) (done bool, since time.Time, err error) {
			return
		})
		graph, err := mock.MainFlow(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

	t.Run("on_src_event", func(t *testing.T) { // MARKER: OnSrcEvent
		assert := testarossa.For(t)

		mock.MockOnSrcEvent(func(ctx context.Context, detail string, origin url.URL) (ok bool, err error) {
			return
		})
		var detail string
		var origin url.URL
		_, err := mock.OnSrcEvent(ctx, detail, origin)
		assert.NoError(err)
	})

	t.Run("reconcile", func(t *testing.T) { // MARKER: Reconcile
		assert := testarossa.For(t)

		mock.MockReconcile(func(ctx context.Context) (err error) {
			return
		})
		err := mock.Reconcile(ctx)
		assert.NoError(err)
	})

	t.Run("on_observe_queue_depth", func(t *testing.T) { // MARKER: QueueDepth
		assert := testarossa.For(t)

		mock.MockOnObserveQueueDepth(func(ctx context.Context) (err error) {
			return
		})
		err := mock.OnObserveQueueDepth(ctx)
		assert.NoError(err)
	})

	t.Run("on_changed_max_items", func(t *testing.T) { // MARKER: MaxItems
		assert := testarossa.For(t)

		mock.MockOnChangedMaxItems(func(ctx context.Context) (err error) {
			return
		})
		err := mock.OnChangedMaxItems(ctx)
		assert.NoError(err)
	})

}
