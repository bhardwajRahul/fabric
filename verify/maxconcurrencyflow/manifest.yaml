# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  hostname: maxconcurrencyflow.verify
  description: maxconcurrencyflow.verify exercises the foreman's adaptive per-task concurrency control via a task that self-emits 429.
  modifiedAt: "2026-05-20T03:09:14Z"

tasks:
  Bounded:
    signature: Bounded(tag string) (tallied bool)
    description: Bounded admits up to cap concurrent in-flight executions and returns 429 above that.
    route: :428/bounded

workflows:
  MaxConcurrency:
    signature: MaxConcurrency(tag string) (tallied bool)
    description: MaxConcurrency defines the single-task graph used to drive the foreman through the backpressure path.
    route: :428/max-concurrency
