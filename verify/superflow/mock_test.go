// Code generated by cmd/genmock. DO NOT EDIT.

package superflow

import (
	"context"
	"testing"

	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/workflow"
	"github.com/microbus-io/testarossa"

	"github.com/microbus-io/fabric/verify/superflow/superflowapi"
)

func TestSuperflow_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("task_a", func(t *testing.T) { // MARKER: TaskA
		assert := testarossa.For(t)

		mock.MockTaskA(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.TaskA(ctx, nil)
		assert.NoError(err)
	})

	t.Run("task_b", func(t *testing.T) { // MARKER: TaskB
		assert := testarossa.For(t)

		mock.MockTaskB(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.TaskB(ctx, nil)
		assert.NoError(err)
	})

	t.Run("task_c", func(t *testing.T) { // MARKER: TaskC
		assert := testarossa.For(t)

		mock.MockTaskC(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.TaskC(ctx, nil)
		assert.NoError(err)
	})

	t.Run("task_d", func(t *testing.T) { // MARKER: TaskD
		assert := testarossa.For(t)

		mock.MockTaskD(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.TaskD(ctx, nil)
		assert.NoError(err)
	})

	t.Run("task_e", func(t *testing.T) { // MARKER: TaskE
		assert := testarossa.For(t)

		mock.MockTaskE(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.TaskE(ctx, nil)
		assert.NoError(err)
	})

	t.Run("task_z", func(t *testing.T) { // MARKER: TaskZ
		assert := testarossa.For(t)

		mock.MockTaskZ(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.TaskZ(ctx, nil)
		assert.NoError(err)
	})

	t.Run("error_handler", func(t *testing.T) { // MARKER: ErrorHandler
		assert := testarossa.For(t)

		mock.MockErrorHandler(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.ErrorHandler(ctx, nil)
		assert.NoError(err)
	})

	t.Run("sub_task_a", func(t *testing.T) { // MARKER: SubTaskA
		assert := testarossa.For(t)

		mock.MockSubTaskA(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.SubTaskA(ctx, nil)
		assert.NoError(err)
	})

	t.Run("sub_task_b", func(t *testing.T) { // MARKER: SubTaskB
		assert := testarossa.For(t)

		mock.MockSubTaskB(func(ctx context.Context, flow *workflow.Flow) (err error) {
			return
		})
		err := mock.SubTaskB(ctx, nil)
		assert.NoError(err)
	})

	t.Run("super", func(t *testing.T) { // MARKER: Super
		assert := testarossa.For(t)

		mock.MockSuper(func(ctx context.Context, flow *workflow.Flow, items []string, useSubgraph bool, behaviors map[string]superflowapi.TaskBehavior) (err error) {
			return
		})
		graph, err := mock.Super(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

	t.Run("super_sub", func(t *testing.T) { // MARKER: SuperSub
		assert := testarossa.For(t)

		mock.MockSuperSub(func(ctx context.Context, flow *workflow.Flow, behaviors map[string]superflowapi.TaskBehavior) (err error) {
			return
		})
		graph, err := mock.SuperSub(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

}
