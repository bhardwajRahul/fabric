# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  hostname: superflow.verify
  description: superflow.verify is a unified workflow fixture covering every transition primitive in a single graph, with per-task behavior driven by state.
  package: github.com/microbus-io/fabric/verify/superflow
  frameworkVersion: 1.28.0
  modifiedAt: "2026-05-22T23:15:20Z"

tasks:
  TaskA:
    signature: TaskA()
    description: TaskA is the entry of the Super graph.
    route: :428/task-a
  TaskB:
    signature: TaskB()
    description: TaskB is the forEach source.
    route: :428/task-b
  TaskC:
    signature: TaskC()
    description: TaskC runs once per forEach element and converges into TaskD's fan-in.
    route: :428/task-c
  TaskD:
    signature: TaskD()
    description: TaskD is the forEach fan-in target with a conditional fan-out.
    route: :428/task-d
  TaskE:
    signature: TaskE()
    description: TaskE is the conditional fan-in target; supports a goto to TaskZ.
    route: :428/task-e
  TaskZ:
    signature: TaskZ()
    description: TaskZ is the goto target out of TaskE.
    route: :428/task-z
  ErrorHandler:
    signature: ErrorHandler()
    description: ErrorHandler handles errors raised by TaskC and rejoins the fan-in.
    route: :428/error-handler
  SubTaskA:
    signature: SubTaskA()
    description: SubTaskA is the entry of the SuperSub subgraph.
    route: :428/sub-task-a
  SubTaskB:
    signature: SubTaskB()
    description: SubTaskB is the second step of the SuperSub subgraph.
    route: :428/sub-task-b

workflows:
  Super:
    signature: Super(items []string, useSubgraph bool, behaviors map[string]TaskBehavior)
    description: Super is the unified workflow graph covering every transition primitive.
    route: :428/super
  SuperSub:
    signature: SuperSub(behaviors map[string]TaskBehavior)
    description: SuperSub is the nested subgraph used by the Super workflow's conditional branch.
    route: :428/super-sub
