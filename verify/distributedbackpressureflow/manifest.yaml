# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  hostname: distributedbackpressureflow.verify
  description: distributedbackpressureflow.verify exercises adaptive concurrency under multi-replica and multi-shard deployment.
  modifiedAt: "2026-05-20T03:55:50Z"

tasks:
  Bounded:
    signature: Bounded(tag string) (tallied bool)
    description: Bounded admits up to cap concurrent in-flight executions and returns 429 above that.
    route: :428/bounded

workflows:
  DistributedBackpressure:
    signature: DistributedBackpressure(tag string) (tallied bool)
    description: DistributedBackpressure routes through the throttled Bounded task; exercises multi-replica + multi-shard backpressure.
    route: :428/distributed-backpressure
