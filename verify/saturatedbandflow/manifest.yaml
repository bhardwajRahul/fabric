# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  hostname: saturatedbandflow.verify
  description: saturatedbandflow.verify exercises the foreman's saturated-band fallthrough in runRefill.
  modifiedAt: "2026-05-20T03:51:09Z"

tasks:
  Bounded:
    signature: Bounded(tag string) (tallied bool)
    description: Bounded admits up to cap concurrent in-flight executions and returns 429 above that.
    route: :428/bounded
  Open:
    signature: Open(tag string) (tallied bool)
    description: Open always succeeds; the unrestricted task at the lower priority band.
    route: :428/open

workflows:
  SaturatedBand:
    signature: SaturatedBand(tag string) (tallied bool)
    description: SaturatedBand routes through the throttled Bounded task.
    route: :428/saturated-band
  OpenBand:
    signature: OpenBand(tag string) (tallied bool)
    description: OpenBand routes through the unrestricted Open task.
    route: :428/open-band
