# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  hostname: switchflow.verify
  description: switchflow.verify exercises AddTransitionSwitch for first-match-wins routing.
  modifiedAt: "2026-05-28T00:49:32Z"

tasks:
  Router:
    signature: Router(amount int) (amountOut int)
    description: Router passes the amount through to state so downstream Switch transitions can match on it.
    route: :428/router
  HandleHigh:
    signature: HandleHigh(amount int) (branch string)
    description: HandleHigh runs when the first switch arm matches (amount>=10000).
    route: :428/handle-high
  HandleMid:
    signature: HandleMid(amount int) (branch string)
    description: HandleMid runs when the second switch arm matches (amount>=1000).
    route: :428/handle-mid
  HandleLow:
    signature: HandleLow(amount int) (branch string)
    description: HandleLow runs as the default switch arm (when="true").
    route: :428/handle-low

workflows:
  Switch:
    signature: Switch(amount int) (branch string)
    description: Switch defines a graph that routes the amount to one of three handlers via a switch ladder ending in a when="true" default.
    route: :428/switch
  SwitchNoMatch:
    signature: SwitchNoMatch(amount int) (branch string)
    description: SwitchNoMatch defines a graph whose final switch arm is when="false", so inputs below the lowest threshold end the flow without routing.
    route: :428/switch-no-match
