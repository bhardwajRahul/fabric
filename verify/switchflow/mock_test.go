// Code generated by cmd/genmock. DO NOT EDIT.

package switchflow

import (
	"context"
	"testing"

	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/workflow"
	"github.com/microbus-io/testarossa"
)

func TestSwitchflow_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("router", func(t *testing.T) { // MARKER: Router
		assert := testarossa.For(t)

		mock.MockRouter(func(ctx context.Context, flow *workflow.Flow, amount int) (amountOut int, err error) {
			return
		})
		var amount int
		_, err := mock.Router(ctx, nil, amount)
		assert.NoError(err)
	})

	t.Run("handle_high", func(t *testing.T) { // MARKER: HandleHigh
		assert := testarossa.For(t)

		mock.MockHandleHigh(func(ctx context.Context, flow *workflow.Flow, amount int) (branch string, err error) {
			return
		})
		var amount int
		_, err := mock.HandleHigh(ctx, nil, amount)
		assert.NoError(err)
	})

	t.Run("handle_mid", func(t *testing.T) { // MARKER: HandleMid
		assert := testarossa.For(t)

		mock.MockHandleMid(func(ctx context.Context, flow *workflow.Flow, amount int) (branch string, err error) {
			return
		})
		var amount int
		_, err := mock.HandleMid(ctx, nil, amount)
		assert.NoError(err)
	})

	t.Run("handle_low", func(t *testing.T) { // MARKER: HandleLow
		assert := testarossa.For(t)

		mock.MockHandleLow(func(ctx context.Context, flow *workflow.Flow, amount int) (branch string, err error) {
			return
		})
		var amount int
		_, err := mock.HandleLow(ctx, nil, amount)
		assert.NoError(err)
	})

	t.Run("switch", func(t *testing.T) { // MARKER: Switch
		assert := testarossa.For(t)

		mock.MockSwitch(func(ctx context.Context, flow *workflow.Flow, amount int) (branch string, err error) {
			return
		})
		graph, err := mock.Switch(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

	t.Run("switch_no_match", func(t *testing.T) { // MARKER: SwitchNoMatch
		assert := testarossa.For(t)

		mock.MockSwitchNoMatch(func(ctx context.Context, flow *workflow.Flow, amount int) (branch string, err error) {
			return
		})
		graph, err := mock.SwitchNoMatch(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

}
