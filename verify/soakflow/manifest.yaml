# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  name: soakflow
  hostname: soakflow.verify
  description: soakflow.verify is a high-volume liveness soak over a complex input-driven workflow.
  package: github.com/microbus-io/fabric/verify/soakflow
  frameworkVersion: 1.31.0
  modifiedAt: "2026-05-31T00:55:27Z"

tasks:
  Seed:
    signature: Seed() (done bool)
    description: Seed normalizes and clamps the random inputs into bounded workflow state.
    route: :428/seed
  FanA:
    signature: FanA() (done bool)
    description: FanA is the dynamic fan-out source.
    route: :428/fan-a
  Work:
    signature: Work() (done bool)
    description: Work processes one fan-out element and contributes a sum-reducer delta.
    route: :428/work
  Collect:
    signature: Collect() (done bool)
    description: Collect is the fan-in target that merges the fan-out contributions.
    route: :428/collect
  Loop:
    signature: Loop() (done bool)
    description: Loop decrements a bounded counter and gotos itself until it reaches zero.
    route: :428/loop
  BoomR:
    signature: BoomR() (done bool)
    description: BoomR always errors; its onError transition routes to Recover.
    route: :428/boom-r
  Recover:
    signature: Recover() (done bool)
    description: Recover is the onError handler that resumes the flow to completion.
    route: :428/recover
  BoomF:
    signature: BoomF() (done bool)
    description: BoomF always errors and has no error transition, so the flow fails.
    route: :428/boom-f
  Join:
    signature: Join() (done bool)
    description: Join is the convergence point before the workflow ends.
    route: :428/join
  InnerEntry:
    signature: InnerEntry() (done bool)
    description: InnerEntry is the single task of the Inner subgraph.
    route: :428/inner-entry
  RunSub:
    signature: RunSub() (done bool)
    description: RunSub invokes the Inner subgraph via flow.Subgraph.
    route: :428/run-sub

workflows:
  Soak:
    signature: Soak() (done bool)
    description: Soak defines the complex, input-driven workflow graph exercised under volume.
    route: :428/soak
  Inner:
    signature: Inner() (done bool)
    description: Inner defines the subgraph invoked by Soak.
    route: :428/inner
