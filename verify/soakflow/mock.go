// Code generated by cmd/genmock. DO NOT EDIT.

package soakflow

import (
	"context"
	"encoding/json"
	"net/http"

	"github.com/microbus-io/errors"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/fabric/sub"
	"github.com/microbus-io/fabric/utils"
	"github.com/microbus-io/fabric/workflow"

	"github.com/microbus-io/fabric/verify/soakflow/soakflowapi"
)

// Mock is a mockable version of the microservice, allowing functions, event sinks and web handlers to be mocked.
type Mock struct {
	*Intermediate
	mockSeed       func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: Seed
	mockFanA       func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: FanA
	mockWork       func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: Work
	mockCollect    func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: Collect
	mockLoop       func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: Loop
	mockBoomR      func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: BoomR
	mockRecover    func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: Recover
	mockBoomF      func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: BoomF
	mockJoin       func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: Join
	mockInnerEntry func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: InnerEntry
	mockRunSub     func(ctx context.Context, flow *workflow.Flow) (done bool, err error) // MARKER: RunSub
	mockSoakGraph  func(ctx context.Context) (graph *workflow.Graph, err error)          // MARKER: Soak
	unsubMockSoak  func() error                                                          // MARKER: Soak
	mockInnerGraph func(ctx context.Context) (graph *workflow.Graph, err error)          // MARKER: Inner
	unsubMockInner func() error                                                          // MARKER: Inner
}

// NewMock creates a new mockable version of the microservice.
func NewMock() *Mock {
	svc := &Mock{}
	svc.Intermediate = NewIntermediate(svc)
	svc.SetVersion(7357) // Stands for TEST
	return svc
}

// OnStartup is called when the microservice is started up.
func (svc *Mock) OnStartup(ctx context.Context) (err error) {
	if svc.Deployment() != connector.LOCAL && svc.Deployment() != connector.TESTING {
		return errors.New("mocking disallowed in %s deployment", svc.Deployment())
	}
	return nil
}

// OnShutdown is called when the microservice is shut down.
func (svc *Mock) OnShutdown(ctx context.Context) (err error) {
	return nil
}

// MockSeed sets up a mock handler for Seed.
func (svc *Mock) MockSeed(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Seed
	svc.mockSeed = handler
	return svc
}

// Seed executes the mock handler.
func (svc *Mock) Seed(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: Seed
	if svc.mockSeed != nil {
		done, err = svc.mockSeed(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockFanA sets up a mock handler for FanA.
func (svc *Mock) MockFanA(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: FanA
	svc.mockFanA = handler
	return svc
}

// FanA executes the mock handler.
func (svc *Mock) FanA(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: FanA
	if svc.mockFanA != nil {
		done, err = svc.mockFanA(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockWork sets up a mock handler for Work.
func (svc *Mock) MockWork(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Work
	svc.mockWork = handler
	return svc
}

// Work executes the mock handler.
func (svc *Mock) Work(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: Work
	if svc.mockWork != nil {
		done, err = svc.mockWork(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockCollect sets up a mock handler for Collect.
func (svc *Mock) MockCollect(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Collect
	svc.mockCollect = handler
	return svc
}

// Collect executes the mock handler.
func (svc *Mock) Collect(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: Collect
	if svc.mockCollect != nil {
		done, err = svc.mockCollect(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockLoop sets up a mock handler for Loop.
func (svc *Mock) MockLoop(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Loop
	svc.mockLoop = handler
	return svc
}

// Loop executes the mock handler.
func (svc *Mock) Loop(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: Loop
	if svc.mockLoop != nil {
		done, err = svc.mockLoop(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockBoomR sets up a mock handler for BoomR.
func (svc *Mock) MockBoomR(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: BoomR
	svc.mockBoomR = handler
	return svc
}

// BoomR executes the mock handler.
func (svc *Mock) BoomR(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: BoomR
	if svc.mockBoomR != nil {
		done, err = svc.mockBoomR(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockRecover sets up a mock handler for Recover.
func (svc *Mock) MockRecover(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Recover
	svc.mockRecover = handler
	return svc
}

// Recover executes the mock handler.
func (svc *Mock) Recover(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: Recover
	if svc.mockRecover != nil {
		done, err = svc.mockRecover(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockBoomF sets up a mock handler for BoomF.
func (svc *Mock) MockBoomF(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: BoomF
	svc.mockBoomF = handler
	return svc
}

// BoomF executes the mock handler.
func (svc *Mock) BoomF(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: BoomF
	if svc.mockBoomF != nil {
		done, err = svc.mockBoomF(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockJoin sets up a mock handler for Join.
func (svc *Mock) MockJoin(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Join
	svc.mockJoin = handler
	return svc
}

// Join executes the mock handler.
func (svc *Mock) Join(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: Join
	if svc.mockJoin != nil {
		done, err = svc.mockJoin(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockInnerEntry sets up a mock handler for InnerEntry.
func (svc *Mock) MockInnerEntry(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: InnerEntry
	svc.mockInnerEntry = handler
	return svc
}

// InnerEntry executes the mock handler.
func (svc *Mock) InnerEntry(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: InnerEntry
	if svc.mockInnerEntry != nil {
		done, err = svc.mockInnerEntry(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockRunSub sets up a mock handler for RunSub.
func (svc *Mock) MockRunSub(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: RunSub
	svc.mockRunSub = handler
	return svc
}

// RunSub executes the mock handler.
func (svc *Mock) RunSub(ctx context.Context, flow *workflow.Flow) (done bool, err error) { // MARKER: RunSub
	if svc.mockRunSub != nil {
		done, err = svc.mockRunSub(ctx, flow)
	}
	return done, errors.Trace(err)
}

// MockSoak sets up a mock handler for the Soak workflow.
// The handler receives typed inputs from the workflow's state and returns typed outputs.
// A nil handler clears the mock.
func (svc *Mock) MockSoak(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Soak
	if svc.unsubMockSoak != nil {
		svc.unsubMockSoak()
		svc.unsubMockSoak = nil
	}
	if handler == nil {
		svc.mockSoakGraph = nil
		return svc
	}
	mockName := "MockSoak" + utils.RandomIdentifier(8)
	mockRoute := ":428/mock-soak-" + utils.RandomIdentifier(8)
	mockTaskURL := httpx.JoinHostAndPath(svc.Hostname(), mockRoute)
	svc.mockSoakGraph = func(ctx context.Context) (graph *workflow.Graph, err error) {
		g := workflow.NewGraph(soakflowapi.Soak.URL())
		g.AddTransition(mockTaskURL, workflow.END)
		return g, nil
	}
	err := svc.Subscribe(mockName, func(w http.ResponseWriter, r *http.Request) error {
		var f workflow.Flow
		if err := json.NewDecoder(r.Body).Decode(&f); err != nil {
			return errors.Trace(err)
		}
		snap := f.Snapshot()
		var in soakflowapi.SoakIn
		f.ParseState(&in)
		done, err := handler(r.Context(), &f)
		if err != nil {
			return err // No trace
		}
		out := soakflowapi.SoakOut{Done: done}
		f.SetChanges(out, snap)
		w.Header().Set("Content-Type", "application/json")
		return json.NewEncoder(w).Encode(&f)
	},
		sub.At("POST", mockRoute),
		sub.Task(soakflowapi.SoakIn{}, soakflowapi.SoakOut{}),
	)
	if err == nil {
		svc.unsubMockSoak = func() error { return svc.Unsubscribe(mockName) }
	}
	return svc
}

// Soak returns the workflow graph, or a mocked graph if MockSoak was called.
func (svc *Mock) Soak(ctx context.Context) (graph *workflow.Graph, err error) { // MARKER: Soak
	if svc.mockSoakGraph != nil {
		graph, err = svc.mockSoakGraph(ctx)
	}
	return graph, errors.Trace(err)
}

// MockInner sets up a mock handler for the Inner workflow.
// The handler receives typed inputs from the workflow's state and returns typed outputs.
// A nil handler clears the mock.
func (svc *Mock) MockInner(handler func(ctx context.Context, flow *workflow.Flow) (done bool, err error)) *Mock { // MARKER: Inner
	if svc.unsubMockInner != nil {
		svc.unsubMockInner()
		svc.unsubMockInner = nil
	}
	if handler == nil {
		svc.mockInnerGraph = nil
		return svc
	}
	mockName := "MockInner" + utils.RandomIdentifier(8)
	mockRoute := ":428/mock-inner-" + utils.RandomIdentifier(8)
	mockTaskURL := httpx.JoinHostAndPath(svc.Hostname(), mockRoute)
	svc.mockInnerGraph = func(ctx context.Context) (graph *workflow.Graph, err error) {
		g := workflow.NewGraph(soakflowapi.Inner.URL())
		g.AddTransition(mockTaskURL, workflow.END)
		return g, nil
	}
	err := svc.Subscribe(mockName, func(w http.ResponseWriter, r *http.Request) error {
		var f workflow.Flow
		if err := json.NewDecoder(r.Body).Decode(&f); err != nil {
			return errors.Trace(err)
		}
		snap := f.Snapshot()
		var in soakflowapi.InnerIn
		f.ParseState(&in)
		done, err := handler(r.Context(), &f)
		if err != nil {
			return err // No trace
		}
		out := soakflowapi.InnerOut{Done: done}
		f.SetChanges(out, snap)
		w.Header().Set("Content-Type", "application/json")
		return json.NewEncoder(w).Encode(&f)
	},
		sub.At("POST", mockRoute),
		sub.Task(soakflowapi.InnerIn{}, soakflowapi.InnerOut{}),
	)
	if err == nil {
		svc.unsubMockInner = func() error { return svc.Unsubscribe(mockName) }
	}
	return svc
}

// Inner returns the workflow graph, or a mocked graph if MockInner was called.
func (svc *Mock) Inner(ctx context.Context) (graph *workflow.Graph, err error) { // MARKER: Inner
	if svc.mockInnerGraph != nil {
		graph, err = svc.mockInnerGraph(ctx)
	}
	return graph, errors.Trace(err)
}
