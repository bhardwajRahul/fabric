# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  hostname: ackdroppedflow.verify
  description: ackdroppedflow.verify exercises the foreman's per-task 404 ack-timeout breaker by deactivating Park's subscription to provoke ack-timeouts.
  modifiedAt: "2026-05-21T04:31:13Z"

tasks:
  Park:
    signature: Park(tag string) (parked bool)
    description: Park returns true; subscription is deactivated by the test to provoke ack-timeouts.
    route: :428/park
  Ping:
    signature: Ping(tag string) (pinged bool)
    description: Ping returns true; always reachable, the unrelated-task control.
    route: :428/ping

workflows:
  AckDropped:
    signature: AckDropped(tag string) (parked bool)
    description: AckDropped defines the single-task graph (park -> END).
    route: :428/ack-dropped
  Echo:
    signature: Echo(tag string) (pinged bool)
    description: Echo defines the single-task graph (ping -> END), the unrelated-task control.
    route: :428/echo
