// Code generated by cmd/genmock. DO NOT EDIT.

package ackdroppedflow

import (
	"context"
	"testing"

	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/workflow"
	"github.com/microbus-io/testarossa"
)

func TestAckdroppedflow_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("park", func(t *testing.T) { // MARKER: Park
		assert := testarossa.For(t)

		mock.MockPark(func(ctx context.Context, flow *workflow.Flow, tag string) (parked bool, err error) {
			return
		})
		var tag string
		_, err := mock.Park(ctx, nil, tag)
		assert.NoError(err)
	})

	t.Run("ping", func(t *testing.T) { // MARKER: Ping
		assert := testarossa.For(t)

		mock.MockPing(func(ctx context.Context, flow *workflow.Flow, tag string) (pinged bool, err error) {
			return
		})
		var tag string
		_, err := mock.Ping(ctx, nil, tag)
		assert.NoError(err)
	})

	t.Run("ack_dropped", func(t *testing.T) { // MARKER: AckDropped
		assert := testarossa.For(t)

		mock.MockAckDropped(func(ctx context.Context, flow *workflow.Flow, tag string) (parked bool, err error) {
			return
		})
		graph, err := mock.AckDropped(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

	t.Run("echo", func(t *testing.T) { // MARKER: Echo
		assert := testarossa.For(t)

		mock.MockEcho(func(ctx context.Context, flow *workflow.Flow, tag string) (pinged bool, err error) {
			return
		})
		graph, err := mock.Echo(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

}
