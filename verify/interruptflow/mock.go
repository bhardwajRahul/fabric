/*
Copyright (c) 2023-2026 Microbus LLC and various contributors

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

	http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by cmd/genmock. DO NOT EDIT.

package interruptflow

import (
	"context"
	"encoding/json"
	"net/http"

	"github.com/microbus-io/errors"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/fabric/sub"
	"github.com/microbus-io/fabric/utils"
	"github.com/microbus-io/fabric/workflow"

	"github.com/microbus-io/fabric/verify/interruptflow/interruptflowapi"
)

// Mock is a mockable version of the microservice, allowing functions, event sinks and web handlers to be mocked.
type Mock struct {
	*Intermediate
	mockTaskA            func(ctx context.Context, flow *workflow.Flow, prompt string) (promptOut string, err error)                // MARKER: TaskA
	mockAwaitInput       func(ctx context.Context, flow *workflow.Flow, userInput string) (userInputOut string, err error)          // MARKER: AwaitInput
	mockCompose          func(ctx context.Context, flow *workflow.Flow, prompt string, userInput string) (result string, err error) // MARKER: Compose
	mockInterruptorGraph func(ctx context.Context) (graph *workflow.Graph, err error)                                               // MARKER: Interruptor
	unsubMockInterruptor func() error                                                                                               // MARKER: Interruptor
}

// NewMock creates a new mockable version of the microservice.
func NewMock() *Mock {
	svc := &Mock{}
	svc.Intermediate = NewIntermediate(svc)
	svc.SetVersion(7357) // Stands for TEST
	return svc
}

// OnStartup is called when the microservice is started up.
func (svc *Mock) OnStartup(ctx context.Context) (err error) {
	if svc.Deployment() != connector.LOCAL && svc.Deployment() != connector.TESTING {
		return errors.New("mocking disallowed in %s deployment", svc.Deployment())
	}
	return nil
}

// OnShutdown is called when the microservice is shut down.
func (svc *Mock) OnShutdown(ctx context.Context) (err error) {
	return nil
}

// MockTaskA sets up a mock handler for TaskA.
func (svc *Mock) MockTaskA(handler func(ctx context.Context, flow *workflow.Flow, prompt string) (promptOut string, err error)) *Mock { // MARKER: TaskA
	svc.mockTaskA = handler
	return svc
}

// TaskA executes the mock handler.
func (svc *Mock) TaskA(ctx context.Context, flow *workflow.Flow, prompt string) (promptOut string, err error) { // MARKER: TaskA
	if svc.mockTaskA != nil {
		promptOut, err = svc.mockTaskA(ctx, flow, prompt)
	}
	return promptOut, errors.Trace(err)
}

// MockAwaitInput sets up a mock handler for AwaitInput.
func (svc *Mock) MockAwaitInput(handler func(ctx context.Context, flow *workflow.Flow, userInput string) (userInputOut string, err error)) *Mock { // MARKER: AwaitInput
	svc.mockAwaitInput = handler
	return svc
}

// AwaitInput executes the mock handler.
func (svc *Mock) AwaitInput(ctx context.Context, flow *workflow.Flow, userInput string) (userInputOut string, err error) { // MARKER: AwaitInput
	if svc.mockAwaitInput != nil {
		userInputOut, err = svc.mockAwaitInput(ctx, flow, userInput)
	}
	return userInputOut, errors.Trace(err)
}

// MockCompose sets up a mock handler for Compose.
func (svc *Mock) MockCompose(handler func(ctx context.Context, flow *workflow.Flow, prompt string, userInput string) (result string, err error)) *Mock { // MARKER: Compose
	svc.mockCompose = handler
	return svc
}

// Compose executes the mock handler.
func (svc *Mock) Compose(ctx context.Context, flow *workflow.Flow, prompt string, userInput string) (result string, err error) { // MARKER: Compose
	if svc.mockCompose != nil {
		result, err = svc.mockCompose(ctx, flow, prompt, userInput)
	}
	return result, errors.Trace(err)
}

// MockInterruptor sets up a mock handler for the Interruptor workflow.
// The handler receives typed inputs from the workflow's state and returns typed outputs.
// A nil handler clears the mock.
func (svc *Mock) MockInterruptor(handler func(ctx context.Context, flow *workflow.Flow, prompt string) (result string, err error)) *Mock { // MARKER: Interruptor
	if svc.unsubMockInterruptor != nil {
		svc.unsubMockInterruptor()
		svc.unsubMockInterruptor = nil
	}
	if handler == nil {
		svc.mockInterruptorGraph = nil
		return svc
	}
	mockName := "MockInterruptor" + utils.RandomIdentifier(8)
	mockRoute := ":428/mock-interruptor-" + utils.RandomIdentifier(8)
	mockTaskURL := httpx.JoinHostAndPath(svc.Hostname(), mockRoute)
	svc.mockInterruptorGraph = func(ctx context.Context) (graph *workflow.Graph, err error) {
		g := workflow.NewGraph(interruptflowapi.Interruptor.URL())
		g.AddTransition(mockTaskURL, workflow.END)
		return g, nil
	}
	err := svc.Subscribe(mockName, func(w http.ResponseWriter, r *http.Request) error {
		var f workflow.Flow
		if err := json.NewDecoder(r.Body).Decode(&f); err != nil {
			return errors.Trace(err)
		}
		snap := f.Snapshot()
		var in interruptflowapi.InterruptorIn
		f.ParseState(&in)
		result, err := handler(r.Context(), &f, in.Prompt)
		if err != nil {
			return err // No trace
		}
		out := interruptflowapi.InterruptorOut{Result: result}
		f.SetChanges(out, snap)
		w.Header().Set("Content-Type", "application/json")
		return json.NewEncoder(w).Encode(&f)
	},
		sub.At("POST", mockRoute),
		sub.Task(interruptflowapi.InterruptorIn{}, interruptflowapi.InterruptorOut{}),
	)
	if err == nil {
		svc.unsubMockInterruptor = func() error { return svc.Unsubscribe(mockName) }
	}
	return svc
}

// Interruptor returns the workflow graph, or a mocked graph if MockInterruptor was called.
func (svc *Mock) Interruptor(ctx context.Context) (graph *workflow.Graph, err error) { // MARKER: Interruptor
	if svc.mockInterruptorGraph != nil {
		graph, err = svc.mockInterruptorGraph(ctx)
	}
	return graph, errors.Trace(err)
}
