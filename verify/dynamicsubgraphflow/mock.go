/*
Copyright (c) 2023-2026 Microbus LLC and various contributors

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

	http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by cmd/genmock. DO NOT EDIT.

package dynamicsubgraphflow

import (
	"context"
	"encoding/json"
	"net/http"

	"github.com/microbus-io/errors"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/fabric/sub"
	"github.com/microbus-io/fabric/utils"
	"github.com/microbus-io/fabric/workflow"

	"github.com/microbus-io/fabric/verify/dynamicsubgraphflow/dynamicsubgraphflowapi"
)

// Mock is a mockable version of the microservice, allowing functions, event sinks and web handlers to be mocked.
type Mock struct {
	*Intermediate
	mockParent               func(ctx context.Context, flow *workflow.Flow, value int, innerDone bool, innerResult int) (parentResult string, err error) // MARKER: Parent
	mockInnerA               func(ctx context.Context, flow *workflow.Flow, value int) (innerStage int, err error)                                       // MARKER: InnerA
	mockInnerB               func(ctx context.Context, flow *workflow.Flow, innerStage int) (innerResult int, innerDone bool, err error)                 // MARKER: InnerB
	mockInnerGraph           func(ctx context.Context) (graph *workflow.Graph, err error)                                                                // MARKER: Inner
	unsubMockInner           func() error                                                                                                                // MARKER: Inner
	mockDynamicSubgraphGraph func(ctx context.Context) (graph *workflow.Graph, err error)                                                                // MARKER: DynamicSubgraph
	unsubMockDynamicSubgraph func() error                                                                                                                // MARKER: DynamicSubgraph
}

// NewMock creates a new mockable version of the microservice.
func NewMock() *Mock {
	svc := &Mock{}
	svc.Intermediate = NewIntermediate(svc)
	svc.SetVersion(7357) // Stands for TEST
	return svc
}

// OnStartup is called when the microservice is started up.
func (svc *Mock) OnStartup(ctx context.Context) (err error) {
	if svc.Deployment() != connector.LOCAL && svc.Deployment() != connector.TESTING {
		return errors.New("mocking disallowed in %s deployment", svc.Deployment())
	}
	return nil
}

// OnShutdown is called when the microservice is shut down.
func (svc *Mock) OnShutdown(ctx context.Context) (err error) {
	return nil
}

// MockParent sets up a mock handler for Parent.
func (svc *Mock) MockParent(handler func(ctx context.Context, flow *workflow.Flow, value int, innerDone bool, innerResult int) (parentResult string, err error)) *Mock { // MARKER: Parent
	svc.mockParent = handler
	return svc
}

// Parent executes the mock handler.
func (svc *Mock) Parent(ctx context.Context, flow *workflow.Flow, value int, innerDone bool, innerResult int) (parentResult string, err error) { // MARKER: Parent
	if svc.mockParent != nil {
		parentResult, err = svc.mockParent(ctx, flow, value, innerDone, innerResult)
	}
	return parentResult, errors.Trace(err)
}

// MockInnerA sets up a mock handler for InnerA.
func (svc *Mock) MockInnerA(handler func(ctx context.Context, flow *workflow.Flow, value int) (innerStage int, err error)) *Mock { // MARKER: InnerA
	svc.mockInnerA = handler
	return svc
}

// InnerA executes the mock handler.
func (svc *Mock) InnerA(ctx context.Context, flow *workflow.Flow, value int) (innerStage int, err error) { // MARKER: InnerA
	if svc.mockInnerA != nil {
		innerStage, err = svc.mockInnerA(ctx, flow, value)
	}
	return innerStage, errors.Trace(err)
}

// MockInnerB sets up a mock handler for InnerB.
func (svc *Mock) MockInnerB(handler func(ctx context.Context, flow *workflow.Flow, innerStage int) (innerResult int, innerDone bool, err error)) *Mock { // MARKER: InnerB
	svc.mockInnerB = handler
	return svc
}

// InnerB executes the mock handler.
func (svc *Mock) InnerB(ctx context.Context, flow *workflow.Flow, innerStage int) (innerResult int, innerDone bool, err error) { // MARKER: InnerB
	if svc.mockInnerB != nil {
		innerResult, innerDone, err = svc.mockInnerB(ctx, flow, innerStage)
	}
	return innerResult, innerDone, errors.Trace(err)
}

// MockInner sets up a mock handler for the Inner workflow.
// The handler receives typed inputs from the workflow's state and returns typed outputs.
// A nil handler clears the mock.
func (svc *Mock) MockInner(handler func(ctx context.Context, flow *workflow.Flow, value int) (innerResult int, innerDone bool, err error)) *Mock { // MARKER: Inner
	if svc.unsubMockInner != nil {
		svc.unsubMockInner()
		svc.unsubMockInner = nil
	}
	if handler == nil {
		svc.mockInnerGraph = nil
		return svc
	}
	mockName := "MockInner" + utils.RandomIdentifier(8)
	mockRoute := ":428/mock-inner-" + utils.RandomIdentifier(8)
	mockTaskURL := httpx.JoinHostAndPath(svc.Hostname(), mockRoute)
	svc.mockInnerGraph = func(ctx context.Context) (graph *workflow.Graph, err error) {
		g := workflow.NewGraph(dynamicsubgraphflowapi.Inner.URL())
		g.AddTransition(mockTaskURL, workflow.END)
		return g, nil
	}
	err := svc.Subscribe(mockName, func(w http.ResponseWriter, r *http.Request) error {
		var f workflow.Flow
		if err := json.NewDecoder(r.Body).Decode(&f); err != nil {
			return errors.Trace(err)
		}
		snap := f.Snapshot()
		var in dynamicsubgraphflowapi.InnerIn
		f.ParseState(&in)
		innerResult, innerDone, err := handler(r.Context(), &f, in.Value)
		if err != nil {
			return err // No trace
		}
		out := dynamicsubgraphflowapi.InnerOut{InnerResult: innerResult, InnerDone: innerDone}
		f.SetChanges(out, snap)
		w.Header().Set("Content-Type", "application/json")
		return json.NewEncoder(w).Encode(&f)
	},
		sub.At("POST", mockRoute),
		sub.Task(dynamicsubgraphflowapi.InnerIn{}, dynamicsubgraphflowapi.InnerOut{}),
	)
	if err == nil {
		svc.unsubMockInner = func() error { return svc.Unsubscribe(mockName) }
	}
	return svc
}

// Inner returns the workflow graph, or a mocked graph if MockInner was called.
func (svc *Mock) Inner(ctx context.Context) (graph *workflow.Graph, err error) { // MARKER: Inner
	if svc.mockInnerGraph != nil {
		graph, err = svc.mockInnerGraph(ctx)
	}
	return graph, errors.Trace(err)
}

// MockDynamicSubgraph sets up a mock handler for the DynamicSubgraph workflow.
// The handler receives typed inputs from the workflow's state and returns typed outputs.
// A nil handler clears the mock.
func (svc *Mock) MockDynamicSubgraph(handler func(ctx context.Context, flow *workflow.Flow, value int) (parentResult string, err error)) *Mock { // MARKER: DynamicSubgraph
	if svc.unsubMockDynamicSubgraph != nil {
		svc.unsubMockDynamicSubgraph()
		svc.unsubMockDynamicSubgraph = nil
	}
	if handler == nil {
		svc.mockDynamicSubgraphGraph = nil
		return svc
	}
	mockName := "MockDynamicSubgraph" + utils.RandomIdentifier(8)
	mockRoute := ":428/mock-dynamic-subgraph-" + utils.RandomIdentifier(8)
	mockTaskURL := httpx.JoinHostAndPath(svc.Hostname(), mockRoute)
	svc.mockDynamicSubgraphGraph = func(ctx context.Context) (graph *workflow.Graph, err error) {
		g := workflow.NewGraph(dynamicsubgraphflowapi.DynamicSubgraph.URL())
		g.AddTransition(mockTaskURL, workflow.END)
		return g, nil
	}
	err := svc.Subscribe(mockName, func(w http.ResponseWriter, r *http.Request) error {
		var f workflow.Flow
		if err := json.NewDecoder(r.Body).Decode(&f); err != nil {
			return errors.Trace(err)
		}
		snap := f.Snapshot()
		var in dynamicsubgraphflowapi.DynamicSubgraphIn
		f.ParseState(&in)
		parentResult, err := handler(r.Context(), &f, in.Value)
		if err != nil {
			return err // No trace
		}
		out := dynamicsubgraphflowapi.DynamicSubgraphOut{ParentResult: parentResult}
		f.SetChanges(out, snap)
		w.Header().Set("Content-Type", "application/json")
		return json.NewEncoder(w).Encode(&f)
	},
		sub.At("POST", mockRoute),
		sub.Task(dynamicsubgraphflowapi.DynamicSubgraphIn{}, dynamicsubgraphflowapi.DynamicSubgraphOut{}),
	)
	if err == nil {
		svc.unsubMockDynamicSubgraph = func() error { return svc.Unsubscribe(mockName) }
	}
	return svc
}

// DynamicSubgraph returns the workflow graph, or a mocked graph if MockDynamicSubgraph was called.
func (svc *Mock) DynamicSubgraph(ctx context.Context) (graph *workflow.Graph, err error) { // MARKER: DynamicSubgraph
	if svc.mockDynamicSubgraphGraph != nil {
		graph, err = svc.mockDynamicSubgraphGraph(ctx)
	}
	return graph, errors.Trace(err)
}
