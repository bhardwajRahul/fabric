# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: Petstore
  hostname: petstore.example
  description: |-
    Petstore delegates to the Swagger Petstore API.
    
    This is a sample Pet Store Server based on the OpenAPI 3.0 specification.  You can find out more about
    Swagger at [https://swagger.io](https://swagger.io). In the third iteration of the pet store, we've switched to the design first approach!
    You can now help us improve the API whether it's by making changes to the definition itself or to the code.
    That way, with time, we can improve the API in general, and expose some of the new features in OAS3.
    
    Some useful links:
    - [The Pet Store repository](https://github.com/swagger-api/swagger-petstore)
    - [The source API definition for the Pet Store](https://github.com/swagger-api/swagger-petstore/blob/master/src/main/resources/openapi.yaml)
  package: github.com/microbus-io/fabric/examples/petstore
  modifiedAt: "2026-06-19T00:03:27Z"

configs:
  RemoteBaseURL:
    signature: RemoteBaseURL() (value string)
    description: RemoteBaseURL is the base URL of the remote Swagger Petstore API.
    validation: url
    default: https://petstore3.swagger.io/api/v3
  BearerToken:
    signature: BearerToken() (value string)
    description: BearerToken is the OAuth2 bearer token presented to the remote Swagger Petstore API.
    secret: true

functions:
  AddPet:
    signature: AddPet(httpRequestBody *Pet) (httpResponseBody *Pet, httpStatusCode int)
    description: |-
      AddPet adds a new pet to the store.
      
      Input:
        - httpRequestBody: httpRequestBody is the pet to add
      
      Output:
        - httpResponseBody: httpResponseBody is the created pet
        - httpStatusCode: httpStatusCode is the remote HTTP status code
    method: POST
    route: /pet
  GetPetById:
    signature: GetPetById(petId int64) (httpResponseBody *Pet, httpStatusCode int)
    description: |-
      GetPetById returns a single pet.
      
      Input:
        - petId: petId is the ID of the pet to return
      
      Output:
        - httpResponseBody: httpResponseBody is the requested pet
        - httpStatusCode: httpStatusCode is the remote HTTP status code
    method: GET
    route: /pet/{petId}

webs:
  UploadFile:
    description: UploadFile uploads an image of the pet.
    method: POST
    route: /pet/{petId}/uploadImage
