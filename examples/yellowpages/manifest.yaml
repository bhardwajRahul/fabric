# Copyright (c) 2023-2026 Microbus LLC and various contributors
# 
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
# 
# 	http://www.apache.org/licenses/LICENSE-2.0
# 
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: Person
  hostname: yellowpages.example
  description: Person persists persons in a SQL database.
  package: github.com/microbus-io/fabric/examples/yellowpages
  modifiedAt: "2026-06-18T23:09:30Z"

configs:
  SQLDataSourceName:
    signature: SQLDataSourceName() (value string)
    description: SQLDataSourceName is the connection string of the SQL database.
    secret: true

outboundEvents:
  OnPersonCreated:
    signature: OnPersonCreated(objKeys []PersonKey)
    description: OnPersonCreated is triggered when persons are created.
    method: POST
    route: :417/on-person-created
  OnPersonDeleted:
    signature: OnPersonDeleted(objKeys []PersonKey)
    description: OnPersonDeleted is triggered when persons are deleted.
    method: POST
    route: :417/on-person-deleted
  OnPersonStored:
    signature: OnPersonStored(objKeys []PersonKey)
    description: OnPersonStored is triggered when persons are stored.
    method: POST
    route: :417/on-person-stored

functions:
  Create:
    signature: Create(obj *Person) (objKey PersonKey)
    description: Create creates a new object, returning its key.
    method: ANY
    route: /create
  Store:
    signature: Store(obj *Person) (stored bool)
    description: Store updates the object.
    method: ANY
    route: /store
  MustStore:
    signature: MustStore(obj *Person)
    description: MustStore updates the object.
    method: ANY
    route: /must-store
  Revise:
    signature: Revise(obj *Person) (revised bool)
    description: Revise updates the object only if the revision matches.
    method: ANY
    route: /revise
  MustRevise:
    signature: MustRevise(obj *Person)
    description: MustRevise updates the object only if the revision matches.
    method: ANY
    route: /must-revise
  Delete:
    signature: Delete(objKey PersonKey) (deleted bool)
    description: Delete deletes the object.
    method: ANY
    route: /delete
  MustDelete:
    signature: MustDelete(objKey PersonKey)
    description: MustDelete deletes the object.
    method: ANY
    route: /must-delete
  List:
    signature: List(query Query) (objs []*Person, totalCount int)
    description: List returns the objects matching the query, and the total count of matches regardless of the limit.
    method: ANY
    route: /list
  Lookup:
    signature: Lookup(query Query) (obj *Person, found bool)
    description: Lookup returns the single object matching the query. It errors if more than one object matches the query.
    method: ANY
    route: /lookup
  MustLookup:
    signature: MustLookup(query Query) (obj *Person)
    description: MustLookup returns the single object matching the query. It errors unless exactly one object matches the query.
    method: ANY
    route: /must-lookup
  Load:
    signature: Load(objKey PersonKey) (obj *Person, found bool)
    description: Load returns the object associated with the key.
    method: ANY
    route: /load
  MustLoad:
    signature: MustLoad(objKey PersonKey) (obj *Person)
    description: MustLoad returns the object associated with the key. It errors if the object is not found.
    method: ANY
    route: /must-load
  BulkLoad:
    signature: BulkLoad(objKeys []PersonKey) (objs []*Person)
    description: BulkLoad returns the objects matching the keys.
    method: ANY
    route: /bulk-load
  BulkDelete:
    signature: BulkDelete(objKeys []PersonKey) (deletedKeys []PersonKey)
    description: BulkDelete deletes the objects matching the keys, returning the keys of the deleted objects.
    method: ANY
    route: /bulk-delete
  BulkCreate:
    signature: BulkCreate(objs []*Person) (objKeys []PersonKey)
    description: BulkCreate creates multiple objects, returning their keys.
    method: ANY
    route: /bulk-create
  BulkStore:
    signature: BulkStore(objs []*Person) (storedKeys []PersonKey)
    description: BulkStore updates multiple objects, returning the keys of the stored objects.
    method: ANY
    route: /bulk-store
  BulkRevise:
    signature: BulkRevise(objs []*Person) (revisedKeys []PersonKey)
    description: |-
      BulkRevise updates multiple objects, returning the number of rows affected.
      Only rows with matching revisions are updated.
    method: ANY
    route: /bulk-revise
  Purge:
    signature: Purge(query Query) (deletedKeys []PersonKey)
    description: Purge deletes all objects matching the query, returning the keys of the deleted objects.
    method: ANY
    route: /purge
  Count:
    signature: Count(query Query) (count int)
    description: Count returns the number of objects matching the query, disregarding pagination.
    method: ANY
    route: /count
  CreateREST:
    signature: CreateREST(httpRequestBody *Person) (objKey PersonKey, httpStatusCode int)
    description: CreateREST creates a new person via REST, returning its key.
    method: POST
    route: /persons
  StoreREST:
    signature: StoreREST(key PersonKey, httpRequestBody *Person) (httpStatusCode int)
    description: StoreREST updates an existing person via REST.
    method: PUT
    route: /persons/{key}
  DeleteREST:
    signature: DeleteREST(key PersonKey) (httpStatusCode int)
    description: DeleteREST deletes an existing person via REST.
    method: DELETE
    route: /persons/{key}
  LoadREST:
    signature: LoadREST(key PersonKey) (httpResponseBody *Person, httpStatusCode int)
    description: LoadREST loads a person by key via REST.
    method: GET
    route: /persons/{key}
  ListREST:
    signature: ListREST(q Query) (httpResponseBody []*Person, httpStatusCode int)
    description: ListREST lists persons matching the query via REST.
    method: GET
    route: /persons
  TryReserve:
    signature: TryReserve(objKey PersonKey, dur time.Duration) (reserved bool)
    description: TryReserve attempts to reserve a person for the given duration, returning true if successful.
    method: ANY
    route: /try-reserve
  TryBulkReserve:
    signature: TryBulkReserve(objKeys []PersonKey, dur time.Duration) (reservedKeys []PersonKey)
    description: |-
      TryBulkReserve attempts to reserve persons for the given duration, returning the keys of those successfully reserved.
      Only persons whose reservation has expired (reserved_before < NOW) are reserved.
    method: ANY
    route: /try-bulk-reserve
  Reserve:
    signature: Reserve(objKey PersonKey, dur time.Duration) (reserved bool)
    description: Reserve unconditionally reserves a person for the given duration, returning true if the person exists.
    method: ANY
    route: /reserve
  BulkReserve:
    signature: BulkReserve(objKeys []PersonKey, dur time.Duration) (reservedKeys []PersonKey)
    description: BulkReserve unconditionally reserves persons for the given duration, returning the keys of those that exist.
    method: ANY
    route: /bulk-reserve

webs:
  Demo:
    description: Demo serves the web user interface for managing persons.
    method: ANY
    route: /demo
