/*
Copyright (c) 2023-2026 Microbus LLC and various contributors

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

	http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by cmd/genservice. DO NOT EDIT.

package creditflow

import (
	"context"
	"net/http"
	"testing"

	"github.com/microbus-io/dwarf/workflow"
	"github.com/microbus-io/errors"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/examples/creditflow/creditflowapi"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/testarossa"
)

func TestCreditflow_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("demo", func(t *testing.T) { // MARKER: Demo
		assert := testarossa.For(t)

		mock.MockDemo(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.Demo(w, r)
		assert.NoError(err)
	})

	t.Run("submit_credit_application", func(t *testing.T) { // MARKER: SubmitCreditApplication
		assert := testarossa.For(t)

		mock.MockSubmitCreditApplication(func(ctx context.Context, flow *workflow.Flow, applicant creditflowapi.Applicant) (applicantName string, ssn string, address string, phone string, employers []string, creditScore int, err error) {
			return
		})
		var applicant creditflowapi.Applicant
		_, _, _, _, _, _, err := mock.SubmitCreditApplication(ctx, nil, applicant)
		assert.NoError(err)
	})

	t.Run("verify_credit", func(t *testing.T) { // MARKER: VerifyCredit
		assert := testarossa.For(t)

		mock.MockVerifyCredit(func(ctx context.Context, flow *workflow.Flow, creditScore int) (creditVerified bool, err error) {
			return
		})
		var creditScore int
		_, err := mock.VerifyCredit(ctx, nil, creditScore)
		assert.NoError(err)
	})

	t.Run("verify_employment", func(t *testing.T) { // MARKER: VerifyEmployment
		assert := testarossa.For(t)

		mock.MockVerifyEmployment(func(ctx context.Context, flow *workflow.Flow, applicantName string, employerName string) (employmentFailuresOut int, err error) {
			return
		})
		var applicantName string
		var employerName string
		_, err := mock.VerifyEmployment(ctx, nil, applicantName, employerName)
		assert.NoError(err)
	})

	t.Run("init_identity_verification", func(t *testing.T) { // MARKER: InitIdentityVerification
		assert := testarossa.For(t)

		mock.MockInitIdentityVerification(func(ctx context.Context, flow *workflow.Flow, applicantName string, ssn string, address string, phone string) (err error) {
			return
		})
		var applicantName string
		var ssn string
		var address string
		var phone string
		err := mock.InitIdentityVerification(ctx, nil, applicantName, ssn, address, phone)
		assert.NoError(err)
	})

	t.Run("verify_s_s_n", func(t *testing.T) { // MARKER: VerifySSN
		assert := testarossa.For(t)

		mock.MockVerifySSN(func(ctx context.Context, flow *workflow.Flow, ssn string) (ssnVerified bool, err error) {
			return
		})
		var ssn string
		_, err := mock.VerifySSN(ctx, nil, ssn)
		assert.NoError(err)
	})

	t.Run("verify_address", func(t *testing.T) { // MARKER: VerifyAddress
		assert := testarossa.For(t)

		mock.MockVerifyAddress(func(ctx context.Context, flow *workflow.Flow, address string) (addressVerified bool, err error) {
			return
		})
		var address string
		_, err := mock.VerifyAddress(ctx, nil, address)
		assert.NoError(err)
	})

	t.Run("verify_phone_number", func(t *testing.T) { // MARKER: VerifyPhoneNumber
		assert := testarossa.For(t)

		mock.MockVerifyPhoneNumber(func(ctx context.Context, flow *workflow.Flow, phone string) (phoneVerified bool, err error) {
			return
		})
		var phone string
		_, err := mock.VerifyPhoneNumber(ctx, nil, phone)
		assert.NoError(err)
	})

	t.Run("identity_decision", func(t *testing.T) { // MARKER: IdentityDecision
		assert := testarossa.For(t)

		mock.MockIdentityDecision(func(ctx context.Context, flow *workflow.Flow, ssnVerified bool, addressVerified bool, phoneVerified bool) (identityVerified bool, err error) {
			return
		})
		var ssnVerified bool
		var addressVerified bool
		var phoneVerified bool
		_, err := mock.IdentityDecision(ctx, nil, ssnVerified, addressVerified, phoneVerified)
		assert.NoError(err)
	})

	t.Run("run_identity_verification", func(t *testing.T) { // MARKER: RunIdentityVerification
		assert := testarossa.For(t)

		mock.MockRunIdentityVerification(func(ctx context.Context, flow *workflow.Flow, applicantName string, ssn string, address string, phone string) (identityVerified bool, err error) {
			return
		})
		var applicantName string
		var ssn string
		var address string
		var phone string
		_, err := mock.RunIdentityVerification(ctx, nil, applicantName, ssn, address, phone)
		assert.NoError(err)
	})

	t.Run("request_more_info", func(t *testing.T) { // MARKER: RequestMoreInfo
		assert := testarossa.For(t)

		mock.MockRequestMoreInfo(func(ctx context.Context, flow *workflow.Flow, reviewAttempts int) (reviewAttemptsOut int, err error) {
			return
		})
		var reviewAttempts int
		_, err := mock.RequestMoreInfo(ctx, nil, reviewAttempts)
		assert.NoError(err)
	})

	t.Run("review_credit", func(t *testing.T) { // MARKER: ReviewCredit
		assert := testarossa.For(t)

		mock.MockReviewCredit(func(ctx context.Context, flow *workflow.Flow, creditScore int, creditVerified bool, reviewAttempts int) (creditVerifiedOut bool, err error) {
			return
		})
		var creditScore int
		var creditVerified bool
		var reviewAttempts int
		_, err := mock.ReviewCredit(ctx, nil, creditScore, creditVerified, reviewAttempts)
		assert.NoError(err)
	})

	t.Run("handle_credit_error", func(t *testing.T) { // MARKER: HandleCreditError
		assert := testarossa.For(t)

		mock.MockHandleCreditError(func(ctx context.Context, flow *workflow.Flow, onErr *errors.TracedError) (creditVerified bool, err error) {
			return
		})
		var onErr *errors.TracedError
		_, err := mock.HandleCreditError(ctx, nil, onErr)
		assert.NoError(err)
	})

	t.Run("decision", func(t *testing.T) { // MARKER: Decision
		assert := testarossa.For(t)

		mock.MockDecision(func(ctx context.Context, flow *workflow.Flow, creditVerified bool, employmentFailures int, identityVerified bool) (approved bool, err error) {
			return
		})
		var creditVerified bool
		var employmentFailures int
		var identityVerified bool
		_, err := mock.Decision(ctx, nil, creditVerified, employmentFailures, identityVerified)
		assert.NoError(err)
	})

	t.Run("identity_verification", func(t *testing.T) { // MARKER: IdentityVerification
		assert := testarossa.For(t)

		mock.MockIdentityVerification(func(ctx context.Context, flow *workflow.Flow, applicantName string, ssn string, address string, phone string) (identityVerified bool, err error) {
			return
		})
		graph, err := mock.IdentityVerification(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

	t.Run("credit_approval", func(t *testing.T) { // MARKER: CreditApproval
		assert := testarossa.For(t)

		mock.MockCreditApproval(func(ctx context.Context, flow *workflow.Flow, applicant creditflowapi.Applicant) (approved bool, creditVerified bool, employmentFailures int, identityVerified bool, err error) {
			return
		})
		graph, err := mock.CreditApproval(ctx)
		if assert.NoError(err) {
			assert.NotNil(graph)
		}
	})

}
