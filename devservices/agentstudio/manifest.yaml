# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  hostname: agentstudio.dev
  description: AgentStudio is a developer console for inspecting flows running under the Foreman.
  package: github.com/microbus-io/fabric/devservices/agentstudio
  frameworkVersion: 1.28.0
  modifiedAt: "2026-06-04T05:35:46Z"

webs:
  ListFlows:
    description: ListFlows renders an HTML page with a paginated, sortable table of flows.
    method: ANY
    route: /flows
  FlowDetail:
    description: FlowDetail renders an HTML page with the details, DAG diagram, and step log of a flow.
    method: ANY
    route: /flows/{flowKey}
  StepDetail:
    description: StepDetail renders an HTML page with the details of one execution step.
    method: ANY
    route: /steps/{stepKey}
  ListWorkflows:
    description: ListWorkflows renders an HTML page listing the workflows available in the system.
    method: ANY
    route: /workflows
  WorkflowDetail:
    description: WorkflowDetail renders an HTML page with the structure and definition of a single workflow graph.
    method: ANY
    route: /workflows/{workflowURL...}
  RunWorkflow:
    description: RunWorkflow renders a form to create and start a workflow with an initial state and FlowOptions.
    method: ANY
    route: /run-workflow
  ContinueFlow:
    description: ContinueFlow renders a form to continue a completed flow's thread with additional state.
    method: ANY
    route: /continue-flow
  ResumeFlow:
    description: ResumeFlow renders a form to resume an interrupted flow with a resume payload.
    method: ANY
    route: /resume-flow
  RestartFlow:
    description: RestartFlow renders a form to restart a terminated flow from its entry step with optional state overrides.
    method: ANY
    route: /restart-flow
  RestartFromStep:
    description: RestartFromStep renders a form to restart a flow from a specific step with optional state overrides.
    method: ANY
    route: /restart-from-step
  PollFlow:
    description: PollFlow returns a JSON status payload driving the FlowDetail live-update progress bar.
    method: GET
    route: /poll-flow
  TaskDetail:
    description: TaskDetail renders an HTML page with the metadata of a single task in a workflow graph.
    method: ANY
    route: /task-detail
  Dashboard:
    description: Dashboard renders an HTML page with operator dashboards for flows and workflows.
    method: ANY
    route: /dashboard
  Assets:
    description: Assets serves the bespa CSS and JavaScript assets at /bespa/.
    method: GET
    route: //bespa/{path...}
