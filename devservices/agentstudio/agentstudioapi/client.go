// Code generated by cmd/genservice. DO NOT EDIT.

package agentstudioapi

import (
	"context"
	"iter"
	"net/http"

	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/fabric/pub"
	"github.com/microbus-io/fabric/service"
)

// Client is a lightweight proxy for making unicast calls to the microservice.
type Client struct {
	svc  service.Publisher
	host string
	opts []pub.Option
}

// NewClient creates a new unicast client proxy to the microservice.
func NewClient(caller service.Publisher) Client {
	return Client{svc: caller, host: Hostname}
}

// ForHost returns a copy of the client with a different hostname to be applied to requests.
func (_c Client) ForHost(host string) Client {
	return Client{svc: _c.svc, host: host, opts: _c.opts}
}

// WithOptions returns a copy of the client with options to be applied to requests.
func (_c Client) WithOptions(opts ...pub.Option) Client {
	return Client{svc: _c.svc, host: _c.host, opts: append(_c.opts, opts...)}
}

// MulticastClient is a lightweight proxy for making multicast calls to the microservice.
type MulticastClient struct {
	svc  service.Publisher
	host string
	opts []pub.Option
}

// NewMulticastClient creates a new multicast client proxy to the microservice.
func NewMulticastClient(caller service.Publisher) MulticastClient {
	return MulticastClient{svc: caller, host: Hostname}
}

// ForHost returns a copy of the client with a different hostname to be applied to requests.
func (_c MulticastClient) ForHost(host string) MulticastClient {
	return MulticastClient{svc: _c.svc, host: host, opts: _c.opts}
}

// WithOptions returns a copy of the client with options to be applied to requests.
func (_c MulticastClient) WithOptions(opts ...pub.Option) MulticastClient {
	return MulticastClient{svc: _c.svc, host: _c.host, opts: append(_c.opts, opts...)}
}

// ListFlows renders an HTML page with a paginated, sortable table of flows.
func (_c Client) ListFlows(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: ListFlows
	if method == "" {
		method = ListFlows.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ListFlows.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ListFlows renders an HTML page with a paginated, sortable table of flows.
func (_c MulticastClient) ListFlows(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: ListFlows
	if method == "" {
		method = ListFlows.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ListFlows.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// FlowDetail renders an HTML page with the details, DAG diagram, and step log of a flow.
func (_c Client) FlowDetail(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: FlowDetail
	if method == "" {
		method = FlowDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, FlowDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// FlowDetail renders an HTML page with the details, DAG diagram, and step log of a flow.
func (_c MulticastClient) FlowDetail(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: FlowDetail
	if method == "" {
		method = FlowDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, FlowDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// StepDetail renders an HTML page with the details of one execution step.
func (_c Client) StepDetail(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: StepDetail
	if method == "" {
		method = StepDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, StepDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// StepDetail renders an HTML page with the details of one execution step.
func (_c MulticastClient) StepDetail(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: StepDetail
	if method == "" {
		method = StepDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, StepDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ListWorkflows renders an HTML page listing the workflows available in the system.
func (_c Client) ListWorkflows(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: ListWorkflows
	if method == "" {
		method = ListWorkflows.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ListWorkflows.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ListWorkflows renders an HTML page listing the workflows available in the system.
func (_c MulticastClient) ListWorkflows(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: ListWorkflows
	if method == "" {
		method = ListWorkflows.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ListWorkflows.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// WorkflowDetail renders an HTML page with the structure and definition of a single workflow graph.
func (_c Client) WorkflowDetail(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: WorkflowDetail
	if method == "" {
		method = WorkflowDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, WorkflowDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// WorkflowDetail renders an HTML page with the structure and definition of a single workflow graph.
func (_c MulticastClient) WorkflowDetail(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: WorkflowDetail
	if method == "" {
		method = WorkflowDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, WorkflowDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// RunWorkflow renders a form to create and start a workflow with an initial state and FlowOptions.
func (_c Client) RunWorkflow(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: RunWorkflow
	if method == "" {
		method = RunWorkflow.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, RunWorkflow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// RunWorkflow renders a form to create and start a workflow with an initial state and FlowOptions.
func (_c MulticastClient) RunWorkflow(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: RunWorkflow
	if method == "" {
		method = RunWorkflow.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, RunWorkflow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ContinueFlow renders a form to continue a completed flow's thread with additional state.
func (_c Client) ContinueFlow(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: ContinueFlow
	if method == "" {
		method = ContinueFlow.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ContinueFlow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ContinueFlow renders a form to continue a completed flow's thread with additional state.
func (_c MulticastClient) ContinueFlow(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: ContinueFlow
	if method == "" {
		method = ContinueFlow.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ContinueFlow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ResumeFlow renders a form to resume an interrupted flow with a resume payload.
func (_c Client) ResumeFlow(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: ResumeFlow
	if method == "" {
		method = ResumeFlow.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ResumeFlow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ResumeFlow renders a form to resume an interrupted flow with a resume payload.
func (_c MulticastClient) ResumeFlow(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: ResumeFlow
	if method == "" {
		method = ResumeFlow.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ResumeFlow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ForkFromStep renders a form to fork a terminal flow from a specific recorded step into a new flow with optional state overrides.
func (_c Client) ForkFromStep(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: ForkFromStep
	if method == "" {
		method = ForkFromStep.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ForkFromStep.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// ForkFromStep renders a form to fork a terminal flow from a specific recorded step into a new flow with optional state overrides.
func (_c MulticastClient) ForkFromStep(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: ForkFromStep
	if method == "" {
		method = ForkFromStep.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, ForkFromStep.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// PollFlow returns a JSON status payload driving the FlowDetail live-update progress bar.
func (_c Client) PollFlow(ctx context.Context, relativeURL string) (res *http.Response, err error) { // MARKER: PollFlow
	return _c.svc.Request(
		ctx,
		pub.Method(PollFlow.Method),
		pub.URL(httpx.JoinHostAndPath(_c.host, PollFlow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Options(_c.opts...),
	)
}

// PollFlow returns a JSON status payload driving the FlowDetail live-update progress bar.
func (_c MulticastClient) PollFlow(ctx context.Context, relativeURL string) iter.Seq[*pub.Response] { // MARKER: PollFlow
	return _c.svc.Publish(
		ctx,
		pub.Method(PollFlow.Method),
		pub.URL(httpx.JoinHostAndPath(_c.host, PollFlow.Route)),
		pub.RelativeURL(relativeURL),
		pub.Options(_c.opts...),
	)
}

// TaskDetail renders an HTML page with the metadata of a single task in a workflow graph.
func (_c Client) TaskDetail(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: TaskDetail
	if method == "" {
		method = TaskDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, TaskDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// TaskDetail renders an HTML page with the metadata of a single task in a workflow graph.
func (_c MulticastClient) TaskDetail(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: TaskDetail
	if method == "" {
		method = TaskDetail.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, TaskDetail.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Dashboard renders an HTML page with operator dashboards for flows and workflows.
func (_c Client) Dashboard(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: Dashboard
	if method == "" {
		method = Dashboard.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Dashboard.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Dashboard renders an HTML page with operator dashboards for flows and workflows.
func (_c MulticastClient) Dashboard(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: Dashboard
	if method == "" {
		method = Dashboard.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Dashboard.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Assets serves the bespa CSS and JavaScript assets at /bespa/.
func (_c Client) Assets(ctx context.Context, relativeURL string) (res *http.Response, err error) { // MARKER: Assets
	return _c.svc.Request(
		ctx,
		pub.Method(Assets.Method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Assets.Route)),
		pub.RelativeURL(relativeURL),
		pub.Options(_c.opts...),
	)
}

// Assets serves the bespa CSS and JavaScript assets at /bespa/.
func (_c MulticastClient) Assets(ctx context.Context, relativeURL string) iter.Seq[*pub.Response] { // MARKER: Assets
	return _c.svc.Publish(
		ctx,
		pub.Method(Assets.Method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Assets.Route)),
		pub.RelativeURL(relativeURL),
		pub.Options(_c.opts...),
	)
}
