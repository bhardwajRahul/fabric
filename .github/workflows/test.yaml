# https://docs.github.com/en/actions/use-cases-and-examples/building-and-testing/building-and-testing-go

name: Test
on:
  pull_request:
    types: [opened, reopened, edited]
  push:
    branches:
      - "main"

jobs:
  test:
    runs-on: ubuntu-latest
    name: Run tests
    env:
      MICROBUS_LOG_DEBUG: "1"
    steps:
      - uses: actions/checkout@v6
      - name: Setup NATS server
        uses: onichandame/nats-action@master
        with:
          port: 4222
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
          cache: true
          cache-dependency-path: go.sum
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Go version
        run: go version
      - name: Python version
        run: python3 --version && python3 -c "import venv; print('venv module OK')"
      - name: Run tests (NATS only)
        run: MICROBUS_SHORT_CIRCUIT=0 go test -count 1 -timeout 60s -cover ./...
      - name: Run tests (short-circuit only)
        run: go test -count 1 -timeout 60s -cover ./...
      - name: Run tests (NATS and short-circuit)
        run: MICROBUS_NATS=nats://127.0.0.1:4222 go test -count 1 -timeout 60s -cover ./...
