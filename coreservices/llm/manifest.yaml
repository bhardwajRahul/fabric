# Copyright (c) 2023-2026 Microbus LLC and various contributors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# 	http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: LLM
  hostname: llm.core
  description: The LLM microservice bridges LLM tool-calling protocols with Microbus endpoint invocations.
  package: github.com/microbus-io/fabric/coreservices/llm
  modifiedAt: "2026-07-02T04:11:38Z"

configs:
  MaxToolRounds:
    signature: MaxToolRounds() (value int)
    description: MaxToolRounds is the maximum number of tool call round-trips per invocation.
    validation: int [1,50]
    default: 10

metrics:
  LLMTokens:
    signature: LLMTokens(value int, provider string, model string, direction string)
    description: LLMTokens counts tokens consumed per LLM turn
    kind: counter
    otelName: microbus_llm_tokens
  ToolCalls:
    signature: ToolCalls(value int, tool_url string, tool_type string, outcome string)
    description: ToolCalls counts tool invocations from the chat loop, labeled by the tool's URL, its feature type (function/web/workflow), and the outcome (ok/error)
    kind: counter
    otelName: microbus_llm_tool_calls

outboundEvents:
  OnResolveProvider:
    signature: OnResolveProvider(model string) (ok bool)
    description: OnResolveProvider is fired by llm.core to resolve which provider serves a given model alias or name. Each provider microservice sinks it and answers ok=true when it is configured (holds an API key) and its catalog recognizes the model. llm.core reads the responder's hostname from the response frame and routes the turn there; the simulated chatbox provider does not subscribe, so it never participates in resolution.
    method: POST
    route: :417/on-resolve-provider

functions:
  Chat:
    signature: Chat(provider string, model string, items []Item, toolURLs []string, options *ChatOptions) (itemsOut []Item, usage Usage, resolvedProvider string)
    description: Chat sends a conversation to an LLM with optional tools, looping through tool calls until the LLM returns a final answer. Provider is a provider microservice hostname (e.g. claude.llm.core); pass it empty or "any" to auto-select a configured provider. Model is a capability-tier alias (fast, default, smart), a provider family alias (e.g. opus), or a concrete model name; empty means default. Each toolURL is the canonical URL of a Microbus Function, Web, or Workflow endpoint exposed to the LLM; Chat fetches each host's OpenAPI document and resolves the URL to a callable tool. On error it still returns the items accumulated before the failure, so a caller running its own retry can resume from them (e.g. wait llmapi.RetryAfter(err) and re-call with the returned items) instead of restarting the conversation.
    method: POST
    route: :444/chat
  Turn:
    signature: Turn(model string, items []Item, tools []Tool, options *TurnOptions) (itemsOut []Item, stopReason string, usage Usage)
    description: Turn executes a single LLM turn. On llm.core it is a stub returning 501 Not Implemented; the actual implementation lives in each provider microservice (claudellm, chatgptllm, geminillm). Call ForHost(<providerHostname>).Turn to reach a specific provider directly, or use Chat for the full conversation loop.
    method: POST
    route: :444/turn

tasks:
  InitChat:
    signature: InitChat(provider string, model string, items []Item, toolURLs []string, options *ChatOptions)
    description: InitChat resolves caller-supplied tool URLs into LLM tool schemas via each host's OpenAPI document and stores them, along with chat options, in flow state for use by the rest of the chat loop.
    route: :428/init-chat
  CallLLM:
    signature: CallLLM(provider string, model string, items []Item, toolResults []ToolResult) (itemsOut []Item, pendingToolCalls []ToolCall, turnUsage Usage)
    description: |-
      CallLLM is the sole owner of the items conversation state key. It folds the prior round's tool
      results (accumulated in the toolResults key by the fan-in) into the conversation, calls the provider,
      and writes the full conversation back to items (plain replace, not a delta).
    route: :428/call-llm
  ProcessResponse:
    signature: ProcessResponse(pendingToolCalls []ToolCall, turnUsage Usage, toolRounds int) (toolsRequested bool, toolRoundsOut int, usageOut Usage)
    description: |-
      ProcessResponse accumulates usage and routes the loop: it fans out one ExecuteTool per pending tool
      call, or ends the loop. It does not write the conversation - CallLLM owns the items state key.
    route: :428/process-response
  ExecuteTool:
    signature: ExecuteTool(currentTool ToolCall) (toolResults []ToolResult)
    description: |-
      ExecuteTool executes a single tool call, identified by the currentTool forEach variable, and returns
      its result. The results of all fanned-out ExecuteTool branches are Append-reduced into the
      toolResults state key, which the next CallLLM folds into the conversation.
    route: :428/execute-tool

workflows:
  ChatLoop:
    signature: ChatLoop(provider string, model string, items []Item, toolURLs []string, options *ChatOptions) (itemsOut []Item, usage Usage)
    description: ChatLoop defines the workflow graph for multi-turn LLM conversations with tool calling.
    route: :428/chat-loop
