# Copyright (c) 2023-2026 Microbus LLC and various contributors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# 	http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: LLM
  hostname: llm.core
  description: The LLM microservice bridges LLM tool-calling protocols with Microbus endpoint invocations.
  package: github.com/microbus-io/fabric/coreservices/llm
  modifiedAt: "2026-06-22T03:43:56Z"

configs:
  MaxToolRounds:
    signature: MaxToolRounds() (value int)
    description: MaxToolRounds is the maximum number of tool call round-trips per invocation.
    validation: int [1,50]
    default: 10

metrics:
  LLMTokens:
    signature: LLMTokens(value int, provider string, model string, direction string)
    description: LLMTokens counts tokens consumed per LLM turn
    kind: counter
    otelName: microbus_llm_tokens_total

functions:
  Chat:
    signature: Chat(provider string, model string, messages []Message, toolURLs []string, options *ChatOptions) (messagesOut []Message, usage Usage)
    description: Chat sends messages to an LLM with optional tools, looping through tool calls until the LLM returns a final answer. On error it still returns the messages accumulated before the failure, so a caller running its own retry can resume from them (e.g. wait llmapi.RetryAfter(err) and re-call with the returned messages) instead of restarting the conversation.
    method: POST
    route: :444/chat
  Turn:
    signature: Turn(model string, messages []Message, tools []Tool, options *TurnOptions) (content string, toolCalls []ToolCall, stopReason string, usage Usage)
    description: Turn executes a single LLM turn. On llm.core this returns 501 Not Implemented; the actual implementation lives in each provider microservice (claudellm, chatgptllm, geminillm).
    method: POST
    route: :444/turn

tasks:
  InitChat:
    signature: InitChat(messages []Message, toolURLs []string, options *ChatOptions) (maxToolRounds int, toolRounds int)
    description: InitChat validates inputs, resolves tool schemas from OpenAPI, and stores them in flow state.
    route: :428/init-chat
  CallLLM:
    signature: CallLLM(provider string, model string, messages []Message) (llmContent string, pendingToolCalls any, turnUsage Usage)
    description: CallLLM sends the current messages and tools to the LLM provider.
    route: :428/call-llm
  ProcessResponse:
    signature: ProcessResponse(llmContent string, turnUsage Usage, toolRounds int, maxToolRounds int) (toolsRequested bool, toolRoundsOut int, usageOut Usage)
    description: ProcessResponse inspects the LLM response, accumulates usage, and routes to the next step.
    route: :428/process-response
  ExecuteTool:
    signature: ExecuteTool()
    description: ExecuteTool executes a single tool call, identified by the currentTool forEach variable.
    route: :428/execute-tool

workflows:
  ChatLoop:
    signature: ChatLoop(provider string, model string, messages []Message, toolURLs []string, options *ChatOptions) (messagesOut []Message, usage Usage)
    description: ChatLoop defines the workflow graph for multi-turn LLM conversations with tool calling.
    route: :428/chat-loop
