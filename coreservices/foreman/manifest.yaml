# Copyright (c) 2023-2026 Microbus LLC and various contributors
# 
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
# 
# 	http://www.apache.org/licenses/LICENSE-2.0
# 
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Code generated by cmd/genmanifest. DO NOT EDIT.

general:
  name: Foreman
  hostname: foreman.core
  description: Foreman orchestrates agentic workflow execution.
  package: github.com/microbus-io/fabric/coreservices/foreman
  frameworkVersion: 1.28.0
  modifiedAt: "2026-05-23T22:51:29Z"

configs:
  SQLDataSourceName:
    signature: SQLDataSourceName() (value string)
    description: SQLDataSourceName is the connection string of the SQL database.
    secret: true
  Workers:
    signature: Workers() (workers int)
    description: Workers is the number of concurrent workers that process flow steps.
    validation: int [1,]
    default: 64
  TimeBudget:
    signature: TimeBudget() (budget time.Duration)
    description: TimeBudget is the hard ceiling on the execution time of any task step. It is applied as the timeout on the task dispatch call; a task endpoint may declare a shorter budget of its own via sub.TimeBudget.
    validation: dur [1s,15m]
    default: 2m
  DefaultPriority:
    signature: DefaultPriority() (defaultPriority int)
    description: DefaultPriority is the priority assigned to a flow when the caller does not specify one. Priority is an integer >= 1, lower numbers run first.
    validation: int [1,]
    default: 5
  NumShards:
    signature: NumShards() (numShards int)
    description: NumShards is the number of database shards. Each shard is a separate database instance. Shards can be added dynamically but never removed.
    validation: int [1,]
    default: 1
    callback: true
  SQLConnectionPool:
    signature: SQLConnectionPool() (value int)
    description: SQLConnectionPool is the number of database connections kept open per shard.
    validation: int [1,]
    default: 8

metrics:
  FlowsStarted:
    signature: FlowsStarted(value int, workflow string)
    description: FlowsStarted counts the number of flows that have been started.
    kind: counter
    otelName: microbus_flows_started_total
  FlowsTerminated:
    signature: FlowsTerminated(value int, workflow string, status string)
    description: FlowsTerminated counts the number of flows that have reached a terminal status.
    kind: counter
    otelName: microbus_flows_terminated_total
  StepsExecuted:
    signature: StepsExecuted(value int, task string, status string)
    description: StepsExecuted counts the number of steps that have been executed.
    kind: counter
    otelName: microbus_steps_executed_total
  StepsQueueDepth:
    signature: StepsQueueDepth(value int)
    description: StepsQueueDepth records the number of steps waiting in the local worker queue.
    kind: gauge
    otelName: microbus_steps_queue_depth
    observable: true
  StepsRecovered:
    signature: StepsRecovered(value int)
    description: StepsRecovered counts the number of steps recovered by pollPendingSteps after lease expiry.
    kind: counter
    otelName: microbus_steps_recovered_total
  StepsPending:
    signature: StepsPending(value int, priority string)
    description: StepsPending records the number of due pending steps in each priority band.
    kind: gauge
    otelName: microbus_steps_pending
    observable: true
  StepsOldestPendingAgeSeconds:
    signature: StepsOldestPendingAgeSeconds(value int, priority string)
    description: StepsOldestPendingAgeSeconds records the age in seconds of the oldest due pending step in each priority band.
    kind: gauge
    otelName: microbus_steps_oldest_pending_age_seconds
    observable: true
  StepsFairnessKeys:
    signature: StepsFairnessKeys(value int, priority string)
    description: StepsFairnessKeys records the number of distinct fairness keys in the most recent refill selection at the given priority band.
    kind: gauge
    otelName: microbus_steps_fairness_keys
  StepsSkippedSaturated:
    signature: StepsSkippedSaturated(value int, task string)
    description: StepsSkippedSaturated counts the number of step admissions skipped because the task was at its current rate-limit ceiling.
    kind: counter
    otelName: microbus_steps_skipped_saturated_total
  TaskRateLimit:
    signature: TaskRateLimit(value int, task string)
    description: TaskRateLimit records the current adaptive per-task dispatch-rate ceiling (ops/sec) derived from the gossiped congestion point.
    kind: gauge
    otelName: microbus_task_rate_limit
    observable: true
  TaskConcurrencyRunning:
    signature: TaskConcurrencyRunning(value int, task string)
    description: TaskConcurrencyRunning records the cluster-wide number of running steps per task; pairs with TaskRateLimit on one panel.
    kind: gauge
    otelName: microbus_task_concurrency_running
    observable: true
  TaskRateCuts:
    signature: TaskRateCuts(value int, task string)
    description: TaskRateCuts counts the additive-decrease cuts to the per-task rate ceiling triggered by a 429 from a task dispatch.
    kind: counter
    otelName: microbus_task_rate_cuts_total
  TaskBreakerTrips:
    signature: TaskBreakerTrips(value int, task string)
    description: TaskBreakerTrips counts the number of times a per-task circuit breaker transitioned to tripped on consecutive 404 ack-timeouts.
    kind: counter
    otelName: microbus_task_breaker_trips_total
  TaskBreakerProbes:
    signature: TaskBreakerProbes(value int, task string, outcome string)
    description: TaskBreakerProbes counts probe attempts against a tripped per-task circuit breaker, labelled by outcome (success or failure).
    kind: counter
    otelName: microbus_task_breaker_probes_total
  TaskBreakerState:
    signature: TaskBreakerState(value int, task string)
    description: "TaskBreakerState records the current state of each per-task circuit breaker: 0 = closed (admitting), 1 = tripped (blocked)."
    kind: gauge
    otelName: microbus_task_breaker_state
    observable: true

outboundEvents:
  OnFlowStopped:
    signature: OnFlowStopped(outcome *workflow.FlowOutcome)
    description: OnFlowStopped is triggered when a flow stops (completed, failed, cancelled, or interrupted). Subscribe with ForHost(svc.Hostname()) to receive notifications for flows started with StartNotify.
    method: POST
    route: :417/on-flow-terminated

functions:
  Create:
    signature: Create(workflowName string, initialState any, opts *workflow.FlowOptions) (flowKey string)
    description: Create creates a new flow for a workflow without starting it.
    method: ANY
    route: :444/create
  Start:
    signature: Start(flowKey string)
    description: Start transitions a created flow to running and enqueues it for execution.
    method: ANY
    route: :444/start
  StartNotify:
    signature: StartNotify(flowKey string, notifyHostname string)
    description: StartNotify transitions a created flow to running with status change notifications sent to the given hostname.
    method: ANY
    route: :444/start-notify
  Snapshot:
    signature: Snapshot(flowKey string) (outcome *workflow.FlowOutcome)
    description: Snapshot returns the current status and state of a flow.
    method: GET
    route: :444/snapshot
  Resume:
    signature: Resume(flowKey string, resumeData any)
    description: Resume resumes an interrupted flow by merging resumeData into the leaf step's state and re-enqueuing it for execution.
    method: POST
    route: :444/resume
  Fork:
    signature: Fork(stepKey string, stateOverrides any, opts *workflow.FlowOptions) (newFlowKey string)
    description: Fork creates a new flow from an existing step's checkpoint.
    method: POST
    route: :444/fork
  Cancel:
    signature: Cancel(flowKey string, reason string)
    description: Cancel cancels a flow that is not yet in a terminal status.
    method: POST
    route: :444/cancel
  History:
    signature: History(flowKey string) (steps []FlowStep)
    description: History returns the step-by-step execution history of a flow.
    method: GET
    route: :444/history
  Retry:
    signature: Retry(flowKey string)
    description: Retry re-executes the last failed step of a flow.
    method: POST
    route: :444/retry
  List:
    signature: List(query Query) (flows []FlowSummary, nextCursor string)
    description: List queries flows by status or workflow name. Set Query.Cursor to the previous call's NextCursor to paginate.
    method: GET
    route: :444/list
  Delete:
    signature: Delete(flowKey string)
    description: Delete removes a flow and its steps from the database. The flow must not be running. Subgraph, fork, and thread lineage references become dangling.
    method: POST
    route: :444/delete
  Purge:
    signature: Purge(query Query) (deleted int)
    description: Purge deletes flows matching the query, except those currently running. Capped at 10000 flows per call.
    method: POST
    route: :444/purge
  ShardInfo:
    signature: ShardInfo() (shards []ShardSummary)
    description: ShardInfo returns per-shard health (latency, row counts, error) for every database shard.
    method: GET
    route: :444/shard-info
  CreateTask:
    signature: CreateTask(taskName string, initialState any) (flowKey string)
    description: CreateTask creates a flow that executes a single task and then terminates, without starting it.
    method: POST
    route: :444/create-task
  Enqueue:
    signature: Enqueue(shard int, stepID int)
    description: Enqueue rings the work doorbell, signalling that a step is pending, so the receiving replica refills or flushes-and-refills its candidate cache.
    method: POST
    route: :444/enqueue
  Await:
    signature: Await(flowKey string) (outcome *workflow.FlowOutcome)
    description: Await blocks until the flow stops (i.e. is no longer created, pending, or running), then returns the status and snapshot.
    method: POST
    route: :444/wait-for-stop
  NotifyStatusChange:
    signature: NotifyStatusChange(flowKey string, status string)
    description: NotifyStatusChange is an internal multicast signal to wake up Await callers across replicas.
    method: POST
    route: :444/notify-status-change
    loadBalancing: none
  BreakBefore:
    signature: BreakBefore(flowKey string, taskName string, enabled bool)
    description: BreakBefore sets or clears a breakpoint that pauses execution before the named task runs.
    method: POST
    route: :444/break-before
  Run:
    signature: Run(workflowName string, initialState any, opts *workflow.FlowOptions) (outcome *workflow.FlowOutcome)
    description: Run creates a new flow, starts it, and blocks until it stops. Returns the terminal status and state.
    method: POST
    route: :444/run
  Continue:
    signature: Continue(threadKey string, additionalState any, opts *workflow.FlowOptions) (newFlowKey string)
    description: Continue creates a new flow from the latest completed flow in a thread, merged with additional state using the graph's reducers. The threadKey can be any flowKey belonging to the thread. The new flow belongs to the same thread and is returned in created status.
    method: POST
    route: :444/continue
  SyncValve:
    signature: SyncValve(taskName string, wCong int, tCong time.Time)
    description: SyncValve broadcasts a per-task congestion point so every replica's admission limit converges from the same anchor.
    method: POST
    route: :444/sync-valve
    loadBalancing: none
  TripBreaker:
    signature: TripBreaker(taskName string)
    description: TripBreaker fires a per-task breaker trip to peer foreman replicas. Each peer stamps its own clock on receipt; closures are not gossiped.
    method: POST
    route: :444/trip-breaker
    loadBalancing: none

webs:
  HistoryMermaid:
    description: HistoryMermaid renders an HTML page with a Mermaid diagram of the flow's execution history.
    method: GET
    route: :444/history-mermaid
