# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: Foreman
  hostname: foreman.core
  description: Foreman orchestrates agentic workflow execution.
  package: github.com/microbus-io/fabric/coreservices/foreman
  modifiedAt: "2026-06-25T11:31:39Z"

configs:
  SQLDataSourceName:
    signature: SQLDataSourceName() (value string)
    description: SQLDataSourceName is the connection string of the SQL database.
    secret: true
  Workers:
    signature: Workers() (value int)
    description: Workers is the number of concurrent workers that process flow steps.
    validation: int [1,]
    default: 64
  TimeBudget:
    signature: TimeBudget() (value time.Duration)
    description: TimeBudget is the default time budget for a single task step's execution, applied as the timeout on the task dispatch call. A flow may override it per-flow via FlowOptions.TimeBudget, up to a hard 15m ceiling; a task endpoint may declare a shorter budget of its own via sub.TimeBudget.
    validation: dur [1s,15m]
    default: 2m
  DefaultPriority:
    signature: DefaultPriority() (value int)
    description: DefaultPriority is the priority assigned to a flow when the caller does not specify one. Priority is an integer >= 1, lower numbers run first.
    validation: int [1,]
    default: 5
  NumShards:
    signature: NumShards() (value int)
    description: NumShards is the number of database shards. Each shard is a separate database instance. Shards can be added dynamically but never removed.
    validation: int [1,]
    default: 1
    callback: true
  SQLConnectionPool:
    signature: SQLConnectionPool() (value int)
    description: SQLConnectionPool is the number of database connections kept open per shard.
    validation: int [1,]
    default: 8

metrics:
  AckTimeouts:
    signature: AckTimeouts(value int, task_url string, outcome string)
    description: "AckTimeouts counts task dispatches that hit a 404 ack-timeout (no microservice acked the dispatch), keyed by the task endpoint and the outcome: \"retry\" when the foreman re-probed within the step's time budget, \"giveup\" when the budget horizon was spent and the step was failed. The \"giveup\" series is the alertable \"a microservice is missing\" signal. Named to parallel the framework's microbus_client_timeout_requests; the Prometheus exporter appends the _total suffix."
    kind: counter
    otelName: microbus_foreman_timeout_requests

outboundEvents:
  OnFlowStopped:
    signature: OnFlowStopped(flowKey string, outcome *workflow.FlowOutcome)
    description: OnFlowStopped is triggered when a flow stops (completed, failed, cancelled, or interrupted). Subscribe with ForHost(svc.Hostname()) for flows created with FlowOptions.NotifyOnStop.
    method: POST
    route: :417/on-flow-terminated

functions:
  Create:
    signature: Create(workflowURL string, initialState any, opts *workflow.FlowOptions) (flowKey string)
    description: Create creates a new flow for a workflow without starting it.
    method: ANY
    route: :444/create
  Start:
    signature: Start(flowKey string)
    description: Start transitions a created flow to running and enqueues it for execution.
    method: ANY
    route: :444/start
  Snapshot:
    signature: Snapshot(flowKey string) (outcome *workflow.FlowOutcome)
    description: Snapshot returns the current outcome of a flow.
    method: GET
    route: :444/snapshot
  Fingerprint:
    signature: Fingerprint(flowKey string) (fingerprint string, status string)
    description: Fingerprint returns a short opaque hash that changes when the flow's status, step count, or any step's updated_at changes — across the flow and any nested subgraph descendants.
    method: GET
    route: :444/fingerprint
  Resume:
    signature: Resume(flowKey string, resumeData any)
    description: Resume continues an interrupted flow, delivering resumeData to the task that armed flow.Interrupt. Fails with 409 if the flow is paused at a breakpoint rather than an interrupt.
    method: POST
    route: :444/resume
  ResumeBreak:
    signature: ResumeBreak(flowKey string, stateOverrides any)
    description: ResumeBreak continues a flow paused at a breakpoint, merging stateOverrides into the leaf step's input state so the about-to-run task observes them. Fails with 409 if the flow is paused at an interrupt rather than a breakpoint.
    method: POST
    route: :444/resume-break
  Cancel:
    signature: Cancel(flowKey string, reason string)
    description: Cancel cancels a flow that is not yet in a terminal status.
    method: POST
    route: :444/cancel
  Restart:
    signature: Restart(flowKey string, stateOverrides any)
    description: Restart wipes everything past a flow's entry step and resets the entry with overrides.
    method: POST
    route: :444/restart
  RestartFrom:
    signature: RestartFrom(stepKey string, stateOverrides any)
    description: RestartFrom sweeps the DAG subtree below a chosen step and resets that step with overrides.
    method: POST
    route: :444/restart-from
  Recover:
    signature: Recover(flowKey string, stateOverrides any)
    description: Recover restarts every failed step of a failed flow, re-running the unhandled failures in one pass.
    method: POST
    route: :444/recover
  History:
    signature: History(flowKey string) (steps []FlowStep)
    description: History returns the step-by-step execution history of a flow.
    method: GET
    route: :444/history
  Step:
    signature: Step(stepKey string) (step *FlowStep)
    description: Step returns the full detail of one execution step, including the state, changes and interrupt payload that History omits.
    method: GET
    route: :444/step
  List:
    signature: List(query Query) (flows []FlowSummary, nextCursor string)
    description: List queries flows by status or workflow URL. Set Query.Cursor to the previous call's NextCursor to paginate.
    method: GET
    route: :444/list
  Delete:
    signature: Delete(flowKey string)
    description: Delete removes a flow and its steps from the database. The flow must not be running. Subgraph and thread lineage references become dangling.
    method: POST
    route: :444/delete
  Purge:
    signature: Purge(query Query) (deleted int)
    description: Purge deletes flows matching the query, except those currently running. Capped at 10000 flows per call.
    method: POST
    route: :444/purge
  ShardInfo:
    signature: ShardInfo() (shards []ShardSummary)
    description: ShardInfo returns per-shard health (latency, row counts, error) for every database shard.
    method: GET
    route: :444/shard-info
  CreateTask:
    signature: CreateTask(name string, taskURL string, initialState any, opts *workflow.FlowOptions) (flowKey string)
    description: CreateTask creates a flow that executes a single task and then terminates, without starting it.
    method: POST
    route: :444/create-task
  Await:
    signature: Await(flowKey string) (outcome *workflow.FlowOutcome)
    description: Await blocks until the flow stops (i.e. is no longer created, pending, or running), then returns the outcome.
    method: POST
    route: :444/wait-for-stop
  BreakBefore:
    signature: BreakBefore(flowKey string, taskName string, enabled bool)
    description: BreakBefore sets or clears a breakpoint that pauses execution before the named task runs.
    method: POST
    route: :444/break-before
  Run:
    signature: Run(workflowURL string, initialState any, opts *workflow.FlowOptions) (outcome *workflow.FlowOutcome)
    description: Run creates a new flow, starts it, and blocks until it stops. Returns the terminal outcome.
    method: POST
    route: :444/run
  Continue:
    signature: Continue(threadKey string, additionalState any, opts *workflow.FlowOptions) (newFlowKey string)
    description: Continue creates a new flow from the latest completed flow in a thread, merged with additional state using the graph's reducers. The threadKey can be any flowKey belonging to the thread. The new flow belongs to the same thread and is returned in created status.
    method: POST
    route: :444/continue
  Signal:
    signature: Signal(op string, payload []byte)
    description: Signal delivers an opaque cross-replica coordination signal (op, payload) to the embedded engine. Excludes self-delivery; processes only signals originating from a peer foreman replica.
    method: POST
    route: :444/signal
    loadBalancing: none

webs:
  HistoryMermaid:
    description: HistoryMermaid renders an HTML page with a Mermaid diagram of the flow's execution history.
    method: GET
    route: :444/history-mermaid
